# Four-language editorials and product analytics — 2026-09-25

> 最新四語詳解驗證與發布進度請見 [2026-09-26 紀錄](CONTINUATION-20260926.md)。以下保留較早日期的歷史紀錄，不代表目前正式站狀態。

## Current verified scope

- Canonical editorial inventory: 430 problems and 430 bilingual teaching texts. Current draft coverage is C++17 395, Python 3 388, C11 353, Java 17 353. **353/430 have all four languages in the working tree**; 231 additional programs and matching Chinese/English code explanations remain. A draft counts as release-ready only after its current source passes the full-case audit on both backends and the Run/publication gates.
- All 371 C++ references that used `bits/stdc++.h` now include standard headers explicitly. `scripts/editorials/explicit-cpp-headers.py --check` compiles all 395 C++17 references with Judge-equivalent compiler flags and rejects any reintroduced catch-all header. This compiler check does not replace full Judge case validation.
- `pnpm check:editorials` now reports per-language coverage. `--require-four` fails until every canonical editorial has all four language keys. This is structure validation, not Judge execution.
- The page can select among *available verified* reference languages. It never displays a generated but unverified program.
- The existing publication gate (`scripts/editorials/publication.ts`) requires each displayed source hash to have full-case AC evidence on Docker and Vercel and Run evidence on both. A source change cannot reuse prose-only revision evidence (`content-revision.ts`). Each batch must add source and both code explanations, run all cases, obtain current oracle/toolchain evidence, then publish an immutable revision.

## Re-run status

- The historical September 22 evidence reports 430 accepted editorial references, 2,174 sample/hidden case files, and 430 ready releases. This request rechecked the archived evidence against today's source/content with its **historical** sandbox ID: 430 ready, 0 pending. That is an offline evidence check, **not a new Judge run or proof for the current sandbox**.
- This request's structural check passed (430/430 parse, 430/430 bilingual). Fresh full-corpus execution has **not** completed. Local PostgreSQL at `localhost:5432` is unavailable. Docker Engine recovered and now supports the local batch audits listed below. The saved Vercel OIDC token had expired; `vercel env run` successfully renewed the context and a **single** Vercel spot check for `uva-439-knight-moves` passed (official AC, two wrong variants rejected). Its new private report is under `generated/editorial-audit-20260925-spotcheck/`. The current local sandbox snapshot ID differs from the historical evidence, so the old release gate correctly marks 430/430 as requiring review under current configuration.
- Thirteen four-language drafts passed their saved sample/hidden cases locally. All 52 language variants also passed full sample/hidden execution and aggregate Submit evaluation in the Vercel Judge Sandbox; their existing incorrect C++ mutations were rejected. The ten additions in this batch are `uva-10079-pizza-cutting`, `uva-11677-alarm-clock`, `uva-11636-hello-world`, `uva-11364-parking`, `uva-11498-division-of-nlogonia`, `uva-10550-combination-lock`, `gpe-10500-brick-wall-patterns`, `uva-913-joana-and-the-odd-numbers`, `uva-13185-dpa-numbers-i`, and `uva-10929-you-can-say-11`. Private reports are under `generated/editorial-audit-20260925/`. The audit previously hit a Java-only false CE on its second compile into a frozen directory; `scripts/editorials/audit.ts` now runs aggregate Submit first and reuses the same compiled program for per-case checks, matching real submissions while avoiding a second compile.
- Ten more four-language drafts passed all saved sample/hidden cases locally and a fresh Vercel Judge full-case audit: 40 new language variants AC, and 20 existing incorrect C++ mutations rejected. These are `uva-11461-square-numbers`, `uva-10783-odd-sum`, `uva-10019-funny-encryption-method`, `uva-10642-can-you-solve-it`, `uva-12250-language-detection`, `uva-12602-nice-licence-plates`, `uva-13055-inception`, `uva-1583-digit-generator`, `uva-591-box-of-bricks`, and `uva-679-dropping-balls`. Both five-problem reports show `passed=5, review=0, missing=0` under `generated/editorial-audit-20260925/batch3a-vercel/` and `batch3b-vercel/`. These checks used the archived `formatting_text` snapshot, not a newly fetched production database snapshot.
- Docker Desktop's UI had started while its Linux Engine socket still refused connections. The engine entered running state at 2026-09-25 17:56:51 Asia/Taipei; `docker version` and `docker info` then succeeded. The 10 additions above passed the Docker full-case audit (`batch3-docker`, 10/10), so they now have both Docker and Vercel audit evidence against the saved `formatting_text` snapshot.
- Continued beyond that checkpoint with 65 more four-language drafts across `batch4` through `batch16`. All 65 passed Docker and Vercel full-case audits, counting corrected one-problem rechecks for batches 11–13. `uva-11401-triangle-counting` and `uva-10057-a-mid-summer-night-s-dream` initially exceeded Python memory; their memory-light revisions passed. `uva-10041-vito-s-family` exposed an uncompilable old wrong-solution mutation; its replacement compiled and was rejected with WA on both backends.
- Added another 34 four-language drafts in `batch17` through `batch23` (five, five, five, four, five, five, five). Docker full-case audit passed all 34, including corrected rechecks for Bachet's Game, Goldbach II, Die Game, Homework, and Steps. Each official variant returned AC and each registered wrong-solution mutation was rejected. At that checkpoint `pnpm check:editorials` reported 122/430 structurally complete, which is not equivalent to passing both Judge backends or publication.
- Added 28 more four-language drafts in `batch24` through `batch29` (five each through batch 28, three in batch 29). All 28 passed Docker, including memory-optimized Java for `uva-10763-foreign-exchange`, a C array-capacity correction for `uva-13190-rockabye-tobby`, and a compilable B2 wrong-solution mutation. Structure now shows 150/430. All batch24–29 Vercel checks remain pending after HTTP 402.
- Added 30 more four-language drafts in `batch30` through `batch38`; `batch31` skipped `uva-12455-bars` because it already had all four languages. Docker passed all 30, counting corrected memory-light implementations for the 500,000-floor Building Designing and near-200,000-segment Line Overlap cases. Batch 38 initially exposed two uncompilable old wrong-solution mutations; after correction, `batch38-mutations-recheck-docker` passed both affected problems, with the wrong solutions rejected as WA. All batch30–38 Vercel checks remain pending after HTTP 402.
- Added 19 more four-language drafts in `batch39` through `batch44` (3, 3, 3, 4, 3, 3). Docker passed all 19 on the archived snapshot: every official C11/C++17/Python3/Java17 source was AC and all registered wrong-solution mutations were rejected. The latest structural check reports 199/430 four-language drafts and 1,027 language programs; 231 problems still lack all four. These batches also await Vercel audit after the HTTP 402 account response.
- Added another 15 four-language drafts in `batch45` through `batch49` (three per batch). Docker passed all 15, counting the `batch48-fix-docker` recheck after the new `uva-10074-take-the-land` Java scanner initially failed compilation. All official variants then returned AC and registered wrong-solution mutations were rejected. The structural check now reports 214/430 complete drafts and 1,072 language programs; 216 problems remain short of four languages. These five batches still need Vercel full-case and Run evidence before publication.
- Added 15 further four-language drafts in `batch50` through `batch54` (three per batch). Docker passed all 15 on the archived snapshot: all official variants returned AC, registered wrong mutations were rejected, and permitted alternate answers were accepted. Structure now reports 229/430 complete drafts and 1,117 language programs; 201 problems remain short of four languages. Vercel and Run checks for these batches remain pending.
- Added 13 further four-language drafts in `batch55` through `batch58` (three, three, three, and four). Docker passed all 13 on the archived snapshot, including all official language sources AC and wrong mutations rejected. The structural check now reports 242/430 complete drafts and 1,156 language programs; 188 problems remain short of four languages. These batches still require Vercel and Run validation before publication.
- Added six more four-language drafts in `batch59` through `batch61` (two per batch). Docker passed all six on the archived snapshot: all official sources returned AC, wrong mutations were rejected, and permitted alternate answers were accepted. The structural check reports 248/430 complete drafts and 1,174 programs; 182 problems remain short of four languages. Vercel and Run evidence remain pending.
- Added 13 more four-language drafts in `batch62` through `batch66` (three, three, three, two, two). Docker passed all 13 on the archived snapshot, counting the `batch64-satellite-fix-docker` recheck. The satellite audit exposed a C11 Judge compiler omission (`-lm`) and an old C++ reference missing `<cmath>`; both were fixed, the reviewed Judge revision updated, and the corrected satellite references and expected mutations passed. Structure now reports 261/430 complete drafts and 1,213 programs; 169 problems remain short of four languages. This Judge pipeline change invalidates older release evidence and requires a fresh release audit. Vercel and Run evidence remain pending.
- Added 14 more four-language drafts in `batch67` through `batch76` (two, one, one, one, one, one, two, two, two, one). Docker passed all 14 on the archived snapshot, including the corrected Java substitution-cipher version in `batch74-cipher-fix-docker` after a non-ASCII hidden case exposed an out-of-bounds character table. Structure now reports 275/430 complete drafts and 1,255 programs; 155 problems remain short of four languages. Each official source returned AC and the registered wrong mutations were rejected. Vercel and Run evidence remain pending.
- Added `uva-532-dungeon-master` and `uva-11624-fire` in `batch77`–`batch78` on September 26. Both passed Docker full-case audits on the archived snapshot, including `batch78-memory-fix-docker` after the million-cell fire map exposed Java memory overrun. The corrected Java BFS uses layers and marks visited cells in its byte grid, and all four language references returned AC. Structure reports 277/430 complete drafts and 1,261 programs; 153 problems remain short of four languages. Vercel and Run evidence remain pending.
- Added ten more four-language drafts in `batch79` through `batch83` (two, one, two, two, three). Docker passed all ten on the archived snapshot, including `batch79-stream-fix-docker` after the large rock-paper-scissors tournament exceeded Python memory with an eager token list. The streaming revision passed. Structure reports 287/430 complete drafts and 1,291 programs; 143 problems remain short of four languages. Vercel and Run evidence remain pending.
- Added 12 more four-language drafts in `batch84` through `batch88` (three, three, two, one, three). Docker passed all 12 on the archived snapshot. The audit checked all four official language sources, rejected registered wrong mutations, and accepted allowed alternate outputs. Structure reports 299/430 complete drafts and 1,327 programs; 131 problems remain short of four languages. Vercel and Run evidence remain pending.
- Added 12 more four-language drafts in `batch89` through `batch95` (two, two, two, one, two, one, two). All 12 passed Docker full-case audits on the archived snapshot, with every official language source AC, registered wrong mutations rejected, and allowed alternatives accepted. Structure reports 311/430 complete drafts and 1,363 programs; 119 problems remain short of four languages. Vercel and Run evidence remain pending.
- Added 11 further four-language drafts in `batch96` through `batch106` (one per batch). All 11 passed Docker full-case audits on the archived snapshot, including the corrected Python backtracking in `batch97-python-fix-docker` after a TLE and Java signed sentinel parsing in `batch105-java-fix-docker` after an RE. Each final official source returned AC and registered wrong mutations were rejected. Structure reports 322/430 complete drafts and 1,396 programs; 108 problems remain short of four languages. Vercel and Run evidence remain pending.
- Added eight more four-language **drafts** in `batch107` through `batch114`. Six (`batch107`–`batch109`, `batch111`–`batch112`, `batch114`) passed full-case Docker audits on the archived snapshot, raising locally Judge-verified four-language coverage to 328/430. `uva-10626-buying-coke` (`batch110`) still exceeds Python time or memory limits under three evaluated implementations, and `uva-12911-subset-sum` (`batch113`) still exceeds Python memory or time limits despite a Java memory fix; neither is Judge-verified in all four languages and neither may be published as such. Structure reports 330/430 four-language drafts and 1,420 programs, with 100 problems still missing language drafts. Vercel and Run evidence remain pending.
- Added eight more four-language drafts in `batch115` through `batch122` (one each). All eight passed Docker full-case audits on the archived snapshot: all four official sources AC, registered wrong mutations rejected, and accepted alternatives preserved. Structure now reports 338/430 four-language drafts and 1,444 programs; 92 problems still lack language drafts. Excluding the unresolved Python failures in `uva-10626-buying-coke` and `uva-12911-subset-sum`, local full-case four-language coverage is 336/430. Vercel and Run evidence remain pending.
- Added eight four-language drafts in `batch123` through `batch130`; all eight passed Docker full-case audits on the archived snapshot after correcting the Python state index for Forwarding Emails, Java parsing for Expression Again, and an existing C++ missing `<tuple>` header for The Tower of Babylon. `batch131` through `batch134` added Tug of War, Prime Land, Gondwanaland Telecom, and Interpreting Control Sequence; all four passed the same full-case Docker audit. Across these 12, every official source returned AC and registered wrong mutations were rejected. Structure now reports 350/430 four-language drafts and 1,480 programs; 80 problems still lack language drafts. Excluding the two unresolved Python failures noted above, local full-case four-language coverage is 348/430. Vercel and Run evidence remain pending.
- Added `uva-657-the-die-is-cast` and `uva-10267-graphical-editor` in `batch135` and `batch136`. Both passed Docker full-case audits on the archived snapshot: all four official languages returned AC, and registered wrong mutations were rejected. Structure now reports 352/430 four-language drafts and 1,486 programs; 78 problems still lack language drafts. The two earlier Python failures remain unresolved, so local full-case four-language coverage is 350/430. Vercel and Run evidence remain pending.
- Added `uva-378-intersecting-lines` in `batch137`, including the previously missing C++ reference. The four language sources passed Docker full-case audit on the archived snapshot; the wrong mutations were rejected and an allowed rounding alternative was accepted. Structure now reports 353/430 four-language drafts and 1,489 programs; 77 problems still lack language drafts. With the two earlier Python failures unresolved, local full-case four-language coverage is 351/430. Vercel and Run evidence remain pending.
- Vercel Sandbox began returning HTTP 402 during batches 17 and 18. Those partial runs do not count as passing either batch. No further external audits were started after the 402; billing/quota access must be restored before their full Vercel check and publication. Existing Docker Judge checks continue locally. The checked corpus remains the archived `formatting_text` snapshot, not a fresh production database export.
- Release review, Run-handler evidence on both backends, current production database reconciliation, publication, and deployment are still pending. The production site does not yet show these new language sets or changed C++ headers.
- Do not claim completion of all four languages, full-corpus fresh Judge validation, publication, or deployment from this checkpoint. Reconnect the sandbox and a current corpus snapshot before running the 430-problem audit. Never treat archived reports as new execution evidence.

## Admin dashboard in this change

- New admin-only `/analytics/product-dashboard` aggregates Taiwan-hour traffic, registrations, logged-in visitors, distinct solvers/AC users, submissions, exam starts, posts/comments, confirmed gross payments, NT$200/NT$2,000 purchases, active/cancelled subscriptions, refund statuses and recent payment/cancellation records.
- `/admin/analytics` renders the new dashboard alongside the existing daily traffic, popular pages, referrers and exam analytics. Confirmed gross excludes authorization holds and is explicitly labelled before refunds/fees. Anonymous unique visitors and true conversion funnels are not measured by the existing pageview model.
- Typecheck, lint, API build, web production build and 238 unit tests passed. Database-backed integration and a production billing reconciliation remain necessary before release; no live transaction was performed.
