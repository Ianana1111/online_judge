The four values are stored as `long long` and sorted ascending. Equality of the first and last then implies all four are equal; otherwise equality within the first and second pairs identifies the rectangle case.

Only after both special forms fail does the program test the strict polygon inequality. Because every operand is already 64-bit, the sum cannot overflow 32-bit before conversion. Exactly one lowercase classification is printed per case.
