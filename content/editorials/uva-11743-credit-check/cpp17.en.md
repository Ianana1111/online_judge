Four string extractions skip separators while retaining each four-character group, producing exactly sixteen digits after concatenation. No numeric card conversion occurs.

Even indices are doubled, and any result above nine subtracts nine before entering the checksum. The final branch prints only the exact `Valid` or `Invalid` word based on divisibility by ten.
