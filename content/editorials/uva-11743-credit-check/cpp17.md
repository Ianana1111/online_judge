四次 cin>>part 會略過組間空格，但每組仍以 string 保存四個字元，所以 0001 不會縮成單一字元。串接後 digits 恰為十六位。

迴圈用 i%2==0 判斷需加倍的位置，value 超過九時減九，等價於將加倍結果的兩位相加。每輪把處理完的 value 加進 sum。最後只根據 sum%10 是否為零輸出大小寫固定的 Valid 或 Invalid，不處理題目以外的真實金流或卡片狀態。
