先把固定要轉的兩圈順時針與一圈逆時針分開，它們共是 `3 × 40 = 120` 格。剩下三段只算有方向的刻度距離：順時針讀值遞減，逆時針讀值遞增，所以依序是 `(start−a+40)%40`、`(b−a+40)%40`、`(b−c+40)%40`。每格是九度，總格數最後乘九。加 40 可讓 C 與 Java 的取餘數運算也得到非負結果。四個值全為零才是終止組，單個零仍是合法刻度。

C 版本以 `scanf` 讀取完整資料，使用題目範圍內足夠的整數型別；每筆答案用 `printf` 單獨輸出。
