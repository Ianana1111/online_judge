`start` 保存輸入的初始刻度，a、b、c 是依序要停靠的密碼。終止條件同時檢查四個數字，不把個別的零誤認為結束。

三個距離變數把各段方向寫在名稱中，便於核對差值的正負。由於任一差值介於 −39 與 39，加上 40 後再 `% 40` 一定得到非負且不足一圈的格數。`ticks` 先放入固定 120 格，再加入三段額外路程；即使 start 等於 a，第一段為零也不影響固定圈數。最後乘九將格數轉成度數，直接輸出整數即可，不必使用圓周率或其他浮點運算。
