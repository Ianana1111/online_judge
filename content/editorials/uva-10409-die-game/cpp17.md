`face` 的六個索引固定代表上、下、北、南、西、東，而不是數字本身。每次讀到一組非零指令數，才重新建立 `{1,6,2,5,3,4}`。

`auto old = face` 複製操作前的狀態。四個分支都只讀 `old`，避免一邊旋轉一邊讀到被覆寫的資料。以 north 分支為例，新的頂面取 `old[3]`，新的底面取 `old[2]`，新的北面取 `old[0]`，新的南面取 `old[1]`，西東兩面保持不變。

輸入保證方向合法，所以前三個分支處理 north、south、west 後，最後分支就是 east。處理完該組的所有指令，只輸出 `face[0]` 和換行。
