token 以字串切成 whole 與 fraction，係數是兩段相接後的整數，所以像 1.0100 仍記得原先四個小數位。整數 ** 計算精確冪，不引入 Decimal 精度設定或二進位浮點近似。

places 是要恢復的小數位總數。zfill 確保切片的左右兩段都有正確位置，接上小數點後才做 rstrip。先刪小數尾零再刪末端點號，最後 lstrip 處理小於一的零；輸出函式回傳純文字，不會再次轉回數值而破壞格式。主迴圈以空白分欄，能接受原題固定欄寬的前置空格。
