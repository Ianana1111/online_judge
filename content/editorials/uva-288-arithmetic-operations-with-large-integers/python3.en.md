The regex recognizes complete decimal literals, `**`, then single operators. Even token positions become Python integers and odd positions operators, preserving the adjacency invariant.

Each splice replaces two neighboring values and removes their joining operator. The maximum power index selects the rightmost chain member; multiplication selects the first remaining one. Final zip pairs each plus/minus with its right operand in order. Lines are processed and printed immediately without accumulating all case intermediates.
