calculate 用正規表示式先辨認完整 **，其餘單字元運算子再各自擷取。偶數位置轉為 Python int，奇數位置保存運算子；第 i 個運算子始終連接 values[i] 與 values[i+1]。

每次切片替換兩個數字並刪除同位置運算子，維持這個對應不變。乘冪迴圈挑最大的索引，所以連續冪從右側開始；乘法用 index 挑第一個。最後 zip 依序配對剩餘運算子與右側數字，− 處理成真正的減法，不取絕對值。主迴圈每次只處理一行並立即輸出，可保留很長的合法輸入而不積累所有案例的中間物件。
