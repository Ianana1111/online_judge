After consuming the test-count newline, `getline` preserves each complete program, including an empty line. `memory{}` and `pointer=0` reset machine state for every case.

All four mutations use nonnegative modulo expressions; dot simply matches no branch. Output explicitly switches the case number to decimal, then bytes to uppercase hexadecimal with zero fill and width two. Decimal mode and space fill are restored afterward.
