讀完組數後忽略該行剩餘換行，再用 getline 逐組讀程式，所以空程式行也能保留。memory{} 讓每組一百格都歸零，pointer 也重新回到起點。

四個狀態變更分支使用非負加數再取模，避免負索引和負格值；點號沒有分支，正好保留狀態。輸出標頭前先設 dec，使 tc 永遠十進位，再切到 hex、uppercase。每格個別設定 setw(2) 並補零，完成後還原 dec 與空格填充，避免格式設定影響下一組。
