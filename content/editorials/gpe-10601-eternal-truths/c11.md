位置相同不一定是同一個狀態：下一次需要走 1、2 或 3 格，會決定哪些出口能走。因此 BFS 的 visited 要記住「位置＋目前階段」，一個格子最多對應三個狀態。起點階段是 0，第一次走 1 格，接著循環為 2、3、1 格。

每一次移動只選一個方向，並逐格檢查途中所有格子；不能因為終點沒有牆，就忽略中間穿過的牆。合法移動才切換到下一階段並加入佇列。所有完整移動的代價都是一次，所以第一次取出出口時的 BFS 距離就是最少移動次數。把狀態編成 3·位置＋階段，用整數陣列保存距離與佇列，避免把三個階段錯誤合併。
