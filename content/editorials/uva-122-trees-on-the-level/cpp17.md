Node 的空 value 表示尚未賦值，題目保證節點值為正，所以不會與合法值混淆。建立孩子時先算整數 next、回寫父節點索引，再 push_back，之後只用整數索引，不保留跨擴容的參考。

() 分支先 BFS 檢查所有節點，再統一輸出。valid 一旦因重複或缺值變 false，就維持到本棵結束，接著 nodes.assign 重建單一空根、valid 重設。解析逗號兩側時，根的路徑可為空字串；值去掉前導零後仍保留其完整十進位內容。
