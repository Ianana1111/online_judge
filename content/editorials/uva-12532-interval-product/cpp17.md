Fenwick 使用一基底，add 透過最低有效位元前進到包含該位置的區間；prefix 反向清掉最低有效位元累加互不重疊的區塊。range 的 left-1 保證左右端點都包含。

C 分支中的 right 此時代表新值 V，兩個比較式轉成整數零或一，相減得到 −1、0 或 1 的更新量。先用舊 value 算完兩個差，再寫回 value，順序不可顛倒。P 分支累加一個字元到 answer，整組命令完成才輸出換行。
