先拿 `10 12` 和 `12 10` 試算：兩組答案都應該是 2，所以不能固定用第二個數減第一個數。讀到一對人數後，比較大小，再用較大者減較小者即可。

題目容許人數到 `2^32`（包含端點），C 的 32 位元 `int` 和 `unsigned int` 都不夠。程式用 `long long` 讀取與計算。`scanf` 回傳 2 才表示讀到完整一組；`0 0` 仍要輸出 0，而非結束輸入。
