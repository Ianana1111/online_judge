a、b 宣告為 long long，讀入階段就能容納最大合法人數；不能先讀入 int 再把差值轉寬，因為讀取已可能失敗。標準 max 與 min 會維持兩個參數的型別，相減也在同一寬型別完成。

while 的條件是兩次讀取都成功，程式不另外判斷數值大小或終止記號，因此零與最大端點都會正常處理。每輪只印一個非負整數和換行，沒有 Case 編號。這份短程式的重點是輸入順序與精確範圍，而不是額外的演算法結構。
