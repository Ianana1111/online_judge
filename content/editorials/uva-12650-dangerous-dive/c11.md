所有出發的潛水員編號是 1 到 N；返回名單只告訴我們哪些人安全回來。建立大小 N+1 的布林表，把每個返回編號標為 true，接著依 1 到 N 的順序找出仍為 false 的編號。按編號順序掃描自然得到題目要的升序輸出，不需要排序。如果全部回來，就輸出星號。輸入可能包含多組直到 EOF；每組都要新建布林表，不能保留上一組的返回紀錄。

`calloc` 將當組標記清零，輸出後 `free`，避免不同案例共用狀態。
