Unconnected matrix entries start at zero, and each diagonal uses a large identity bottleneck. Duplicate roads update with max in both directions.

`via` is the outer Floyd loop, preserving the intermediate-set invariant. The guide seat is subtracted once from the complete route bottleneck, not once per edge. Integer division plus remainder implements the ceiling without floating point, and each scenario ends with the exact blank-line format.
