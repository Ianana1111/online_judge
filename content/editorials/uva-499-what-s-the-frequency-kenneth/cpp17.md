alphabet 明確列出大寫再小寫的順序，不依賴 locale 的字母排序。讀入字元先以 unsigned char 表示，只有落在 ASCII 英文字母區間才索引 count，其他字元直接忽略，行尾 CR 也不計入。

best 與陣列都在每一輪 getline 內重建，避免跨行累積。輸出迴圈要求 best>0，因此沒有字母或完全空白的行，仍只輸出空列表、分隔空格與零。一般行則逐個印出所有並列最高者，不在字母之間額外插入空格。
