`while (cin >> n >> m)` 只以 EOF 結束，包含 `0 0` 的資料也會進入迴圈。第一個分支先排除 n≤1、m≤1，讓後面的 `% m` 與 `/ m` 都安全，且每次成功除法都能確保進展。

`sequence` 從 n 開始；每次確認能整除，才更新 current 並加入下一項。離開迴圈後，以 `current != 1` 判斷整組失敗，這時尚未輸出任何序列內容。

成功分支逐項輸出，第一項前沒有空格，後續項目前各印一個空格。long long 能安全容納輸入與中間值，但這個方法始終只除不乘，數值不會變大。
