先列出上界十萬以內的所有正平方數，從 `1²` 到 `316²` 即可；`317²` 已超出題目範圍。每筆查詢只要逐一檢查哪些平方數落在閉區間 `[a,b]`，兩個端點若剛好是平方數也必須算進去。候選只有 316 個、查詢最多兩百筆，直接掃描足夠快，也完全不必擔心浮點數開根號的捨入誤差。只有 `0 0` 這一對是終止標記，不能把其他含零的資料當成新答案。

python3 程式把這個推理直接轉成計算或查表，並依題目規定讀取每一組資料與輸出答案。
