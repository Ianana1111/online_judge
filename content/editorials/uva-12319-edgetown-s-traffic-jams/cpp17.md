readGraph 用 getline 保留每個路口的一整行，行首是來源編號，後方可有零個或多個鄰居，並不額外讀鄰居數。矩陣只設 g[u][v]=1，方向完全照輸入；對角預設零。

兩次呼叫各回傳已完成 Floyd 的獨立矩陣，避免新圖覆寫原距離。INF 選得远高於合法路徑且相加不溢位，驗證時仍明確判斷不可達。diameter 只從 old 更新，valid 一旦失敗不再恢復，最後輸出 Yes／No、空格與完整原網直徑。
