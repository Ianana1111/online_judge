An exchange removes three empties and returns one after the drink, for a net cost of two. The original `N` drinks leave `N` empties, allowing `N / 2` more drinks. With two empties left, one borrowed bottle enables the last exchange and can be repaid afterward.

Print `N + N / 2` for each input value. Java integer division gives the required floor, and `hasNextInt()` continues until EOF.
