sums 先放入空選法的零，再以 size 固定本輪之前的清單長度，因此新加入的元素只會被選一次。reserve 事先配置最多 2^(半部長度) 個值，避免倍增時反覆重配。清單排序但不 unique，保留每種組合的數量。

i 從左表開頭、j 從右表尾端開始，遇到相等的和分組統計。leftCount、rightCount、answer 都是 long long，乘法不會先在窄型別溢位。target==0 時只減一次，對應兩半都不選，並不移除其他合法零和組合。每個 EOF 案例都重新建立兩張清單與答案。
