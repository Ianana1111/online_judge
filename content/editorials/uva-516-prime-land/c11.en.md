Each line describes one whole integer through prime/exponent pairs. Start at one and multiply each `p^e` back in. The C solution keeps line boundaries with `fgets` and parses pairs with `strtol`, so it cannot accidentally consume the next case. The original value is at most 32767, making integer multiplication sufficient.

Subtract one from the reconstructed value, then trial-divide from 2, recording how many times each divisor divides. If the remaining value exceeds one after the divisor-squared test, it is the final prime factor. Trial division discovers factors in ascending order, so print the saved pairs backward. Decrementing individual input factors would not represent subtracting one from the number.
