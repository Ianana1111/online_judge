每行的質數與指數描述的是一個完整整數，先從 1 開始把各個 `p^e` 乘回去。C 版用 `fgets` 保留資料行邊界，再用 `strtol` 依序讀取成對數字，避免把下一行誤接到本行。題目保證原數不超過 32767，整數乘法足夠。

把還原的數減一後，從 2 開始試除並記錄每個因數的次數。當除數平方超過剩餘數時，若還有大於 1 的剩餘值，它本身就是最後的質因數。試除結果由小到大，最後倒序印出；不能對輸入中的各個質數直接減一。
