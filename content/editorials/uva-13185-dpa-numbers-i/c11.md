真因數是可以整除 N、但不包含 N 自己的正整數，因此迴圈從一走到 `N−1`，遇到 `N % divisor == 0` 就加入總和。題目上限只有一千，直接照定義枚舉比質因數分解更容易檢查，也足夠快。算完後比較總和與 N：較小輸出 `deficient`、相等輸出 `perfect`、較大輸出 `abundant`。例如八的真因數是一、二、四，和為七，所以屬於 deficient。先讀組數，恰好輸出同樣數量的分類。

這份 c11 程式將上述推導直接寫成迴圈或公式，並保留題目要求的輸入結束與輸出格式。
