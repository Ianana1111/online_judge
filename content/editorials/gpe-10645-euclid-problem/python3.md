先不要把擴展歐幾里得想成一串需要背的公式。每次除法都把餘數寫成 a、b 的線性組合，所以迴圈除了保存兩個餘數，也保存各自的 x、y 係數。最後非零餘數就是 gcd，它的係數先給我們一組可行解。

但題目還要最小化 |x|+|y|。所有解可以寫成 x=x0+k·b/g、y=y0−k·a/g；這是兩個絕對值組成的凸函數。斜率只會在 x 或 y 穿過零時改變，因此檢查兩個零點左右的整數 k，外加原解，就足夠了。負數除法要用向下取整，不能直接套 C 或 Java 的截斷除法。最後依總絕對值、x≤y、係數大小依序比較。
