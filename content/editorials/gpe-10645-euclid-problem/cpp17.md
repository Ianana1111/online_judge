r0、r1 是相鄰兩個歐幾里得餘數，x0/y0 與 x1/y1 分別是它們對 A、B 的係數。先算完 r2、x2、y2 才一起搬移，避免覆蓋仍需使用的舊數值。

floorDiv 的分母固定為正數；餘數小於零時，C++ 的截斷商須再減一。兩個零點相鄰的整數放進 shifts，重複候選不影響正確性。key 的第一欄是最小化目標，第二欄以 false 優先，因此同樣大小時選 X≤Y。最後輸出的 r0 就是原本兩數的最大公因數。
