`sums` 只在 j 大於 i 時加入一個和，保證使用不同位置。排序後允許保留重複的和，因為重複不改變最近數值，也不影響二分搜尋。

每筆查詢先由 lower_bound 取得右側候選；若沒有右側，改用 back()。只有 iterator 不等於 begin 時才取前一個值，避免越界。`llabs` 比較兩個距離，計算使用 long long，避免一般 32 位元加減過窄。案例標頭在讀查詢前輸出一次；每筆答案都保留題目要求的句末句點。
