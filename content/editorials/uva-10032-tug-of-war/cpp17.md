每個 possible[count] 是一整列重量可達位元，左移 w 代表所有原有重量都加上此人的重量。seen 限制迴圈不去處理不可能超過已讀人數的列，但不改變演算法的完整性。

最後只查看 teamSize 那一列，卻遍歷零到 total 的完整重量範圍，避免人數少卻較重的最佳解遺漏。low、high 在找到更小差距時由 min、max 組出，與先前選的是哪一隊無關。不同案例以額外空行分隔，輸入則用串流讀取自然略過空白行。
