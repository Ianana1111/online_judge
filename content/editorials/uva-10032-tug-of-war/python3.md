Python 的整數可以當作不限長度的位元集合。`possible[count]` 的第 `sum` 個位元表示選 `count` 人、重量為 `sum` 是否可行。新成員重量為 `w` 時，將 `possible[count-1] << w` 合併進來；`count` 必須倒序更新，以免同一人重複使用。

只需選出 `n//2` 人，另一隊就是補集。掃描該列所有可行重量，找出與補集重量差最小的分法。奇數人時，少人的那隊可能更重，因此不能只看半數總重以下。
