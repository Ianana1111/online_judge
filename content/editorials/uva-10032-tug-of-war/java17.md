Java 的 `BigInteger` 可用位元操作實現與 C++ `bitset` 相同的轉移。`possible[count].testBit(sum)` 表示恰好選 `count` 人且重量為 `sum`。讀到重量 `w`，把前一列 `shiftLeft(w)` 並 `or` 進目前列；人數由大到小走，確保每人最多取一次。

完成後列舉選 `n/2` 人時所有可達重量，與剩餘隊伍比較差距。即使所選隊伍人數較少，重量也可能較大，故要掃完整範圍。輸出時再將兩隊重量排成小到大。
