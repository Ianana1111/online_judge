把「選出幾人」放在第一維、總重放在位元位置。C 沒有標準大位元集合，所以每列用 704 個 `uint64_t`，足以涵蓋 0 到 45000 的重量。加入重量 `w` 時，把前一人數列左移 `w`：拆成完整 64 位元字數與剩餘位數，並合併跨字邊界的高位。人數倒序更新，才不會把同一人選兩次。

最後只看恰好 `n/2` 人的列，掃描**全部**可達重量。奇數人時，人數少的一隊仍可能比較重，故不能只掃總重一半以下。比較 `|total-2*sum|`，輸出較小與較大的隊伍重量。
