All containers and flags are constructed inside the case loop. Short-circuit checks avoid reading top or front when empty, and each pop is separately guarded.

Even eliminated candidates may continue structural simulation, but their flags never return to true. Adding Boolean flags gives the survivor count; zero and multiple cases are handled before selecting the sole remaining name.
