篩表先排除零與一，再刪去每個質數平方起的倍數。primes 依數值遞增建立，所以任何下標連續片段就恰好是題目要求的連續質數。

每個 target 重新設定 left、sum、answer。右端加值後的 while 可以移除多個左端，保證比較時總和不大於目標；因 target≥2 且只加入不超過 target 的正質數，視窗不會在相等判斷時變成零長度解。命中後不特別重設，下一個右端會自然繼續搜尋。
