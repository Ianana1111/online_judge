Morning routes use ascending iterators, while reverse iterators sort evening routes descending. Equal indices therefore form opposite-order pairs and use every route once.

For each driver, `max(0, sum-limit)` computes nonnegative overtime before multiplication by `rate`. The leading `1LL` promotes the multiplication to 64 bits, and `cost` is reset for each dataset. The zero sentinel is checked before reading route arrays.
