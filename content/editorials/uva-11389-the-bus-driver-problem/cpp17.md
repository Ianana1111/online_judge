morning 使用一般迭代器升序排序，evening 使用反向迭代器，等效於將內容排成降序。相同索引因此構成短早班搭長晚班，兩組每條路線都恰好使用一次。

每個索引先計算兩條長度減 limit，max(0,...) 保留非負加班量，再乘 rate 加到 cost。1LL 確保後續乘法使用 long long，cost 每組歸零。while 讀完 n、limit、rate，再在三零終止案例的 n=0 停止，不會繼續讀不存在的路線。
