程式先讀所有查詢，以最大的 n/2 決定階乘表長度。factorial[0] 初始化為一，讓 n=1 自然得到正確結果。每一步 factorial[i−1]*i 都以 long long 計算並立即取模。

對每個 n 取 f=(floor(n/2))!，先得到 f²；若 n 為奇數，再乘 n，正好合併中間值放中央、左側、右側的三類計數。因模數約十億，f*f 仍在 signed 64 位元上限內。輸入不含案例數或零哨兵，迴圈必須直接讀到 EOF，並保持原查詢順序輸出。
