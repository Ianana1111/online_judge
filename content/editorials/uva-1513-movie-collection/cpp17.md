Fenwick 保存的是零或一的占用量，不是電影編號。position 陣列讓每次請求能直接找到目前位置，不需搜尋堆疊。m 個預留位置都小於初始的 m+1，因此新 top 一定在所有電影之前。

每次先讀前綴並輸出，再對舊位置加 −1，改 position，最後新位置加一。top 初始 m，每次使用後減一，最後一次使用一，不會對零做 add。輸出用 request>0 決定前置空白，整個案例只有一行，與題面要求一致。
