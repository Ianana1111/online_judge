外層輸入迴圈以 `value != 0` 排除終止符。第二層 `while (value >= 10)` 表示目前至少兩位，必須繼續轉換；若一開始只有一位，就直接輸出。

每輪建立新的 sum，最內層迴圈依序用 `% 10` 取得位數、`/ 10` 去掉位數。該輪結束時 value 已經變成 0，因此用 `value=sum` 接續下一輪，不能漏掉這個指定。

本題最大輸入仍能放在有號 32 位元整數內，程式使用 `long long` 讓讀取與除法更直接。最後一次轉換完成後 value 介於 1 到 9，主程式只輸出這個值與換行，不需要額外計算模數。
