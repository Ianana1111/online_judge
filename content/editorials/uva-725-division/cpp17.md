uniqueDigits 在處理兩個數時共用 used，而且每個數都固定迭代五次。每次先檢查對應 bit 是否已存在，再設一，因此無法把重複數字抵銷或忽略。最後完整遮罩 1023 明確表示十個數字全部出現。

setfill('0') 搭配 setw(5) 只對兩個數字欄位補零，其他字串不受欄寬影響。found 決定本組是否需要無解句，first 則只控制不同查詢間的空白行，避免把空行插在同一組的兩個解之間。
