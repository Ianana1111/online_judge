getline 讀取組數後，外圈處理指定組數。內圈只在已讀到樹木後遇到空行才結束這組；組首空行會跳過。CRLF 的 CR 只從行末移除，名稱中的空格完整保留。

counts 使用有序 map，long long 保存棵數和放大分子。scaled 表示百分比再乘 10000 的整數；除以 10000 得到整數部分，取餘數得到小數部分。setw(4) 搭配 setfill('0') 把小數補滿四位，印完立即還原空格填充。外圈只在第二組以後印空行，避免改變題目要求的案例分隔。
