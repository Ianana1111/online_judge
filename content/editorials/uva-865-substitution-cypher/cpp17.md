getline 先取案例數所在整行，再略過案例前的空字串。plain 和 sub 保留完整字元內容；映射陣列用 unsigned char 索引，避免 char 的正負號使位元組值變成負索引。

映射預設 c→c，再逐位置覆寫指定字母。文字讀取的停止條件是 line.empty，而不是清除空白後再判空，所以只含空格的行仍是內容。每行轉換時以原順序逐字查表，行尾另外輸出換行。tc 非零時先印一個空行分隔案例，接著依指定順序輸出 sub、plain 與文字。
