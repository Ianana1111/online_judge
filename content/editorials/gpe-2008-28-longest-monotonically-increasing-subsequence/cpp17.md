up 的計算從 n−1 往零走，保證每個後方 up[j] 都已完成。values 使用無符號寬整數，可容納 2^32−1，避免用帶符號 32 位元讀取上界時溢位。

visit 的 start 保證索引嚴格遞增，path.back 比較保證數值嚴格遞增。每次先 push 候選、遞迴、再 pop 還原，兩條分支不會互相污染；remaining 為零時保存完整副本。answers 保留每次索引選擇，不做集合去重，因此相同數值的合法多重結果仍在。第一行用 answers.size，後面每行依原路徑順序列出數字。
