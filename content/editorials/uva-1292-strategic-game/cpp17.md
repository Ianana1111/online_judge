讀取 `u:(k)` 時用三個字元變數吃掉冒號與括號，再讀恰好 k 個鄰居。每條道路加入兩個方向，parent 與 order 才能從任意根走訪整棵樹，不依賴輸入行順序。

on 初始化一、off 初始化零，代表當前頂點的選擇成本。order 反向使所有子節點已算完，只有 parent[v]==u 的鄰居才算子節點，避免把父邊重複累加。最後 min(off[0],on[0]) 選根的最佳狀態；n=1 時無任何邊，正確輸出零。
