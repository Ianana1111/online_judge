total 的 501 個位置先全部設為零，因此 0 和 1 都代表沒有數對的基礎情況。外圈 right 依序增加，先繼承前一個前綴答案，再枚舉所有嚴格較小的 left。

std::gcd 的兩個參數都是正整數，回傳正的最大公因數；每次加進 long long 的 total[right]。預處理完全不依賴查詢順序，所以後面的 while 可以讀取任意大小、重複的 N，直接輸出對應結果。只有單獨零是終止標記，不輸出 total[0]。
