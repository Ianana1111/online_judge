Frame 依序保存 u、v、n，每個向量都是三個小整數，因此全部運算精確，不需要浮點數或三角函數。turn 用四種九十度旋轉把目前格方向轉成鄰格方向，negative 只負責反向。

seen 與 frame 分開儲存，避免尚未設定的向量被拿來比較。BFS 每次取出一格才增加 visited 並收集法向量；已走訪鄰格仍會比對完整 Frame。最後同時要求 valid、visited 等於六、六種不同法向量，才輸出 correct。矩陣用整數串流讀入，自動略過案例間的空白行。
