decode_message 每次建立 position=0，因此不同訊息不共享金鑰進度。空格分支只前進 i，保留 position；wrapped 使用短路判斷，空金鑰或剩餘長度不足時不索引不存在的字元，也不做模零。

包裹分支必須通過中間字元的集合檢查，普通分支則使用相反的集合條件。這兩種規則剛好對應原始加密規則；只有普通分支也不合法才回傳錯誤訊息。讀取案例時先處理分隔空行，若空字母集合的下一行已是數字位移則保留該空金鑰。讀到 count 後，按精確行數取訊息，不用跳過空行的方式讀訊息。最後每個案例內每則一行，案例間加入一個空白行。
