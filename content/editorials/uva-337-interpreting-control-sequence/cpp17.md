讀完 n 先用 getline 消耗行尾，接下來正好讀 n 行，包含空白與空行；行與行之間不修改游標。at 在控制序列中主動前進，兩位數座標分別讀入 row 與 col，不會當成要印的字元。

非字面控制命令處理後 continue，只有 ^^ 把 ch 設成 ^ 再走共用寫入路徑。insert 的搬移從 c=9 往左進行，保護尚未搬走的原字元。screen 的每列永遠長十，輸出不做 trim，因為外框內的空白都是畫面內容。每組重新建立狀態，案例編號則持續累加。
