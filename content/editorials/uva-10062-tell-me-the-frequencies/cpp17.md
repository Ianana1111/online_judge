getline 去掉 LF，但不保證去掉 CR，因此程式只移除行末的 CR，不刪除真正的前後空格。計數使用 unsigned char 取得非負代碼，合法輸入都落在大小 128 的陣列內。

codes 只收集實際出現的代碼，排序函式在 count 不同時立即比較升序，同頻才比較代碼降序。first 只追蹤是否已處理過一個輸入行，即使那行是空字串，也會更新成 false；後續輸出照案例關係加入分隔換行。最後印整數 ch 及其次數，並保持每筆資料一行。
