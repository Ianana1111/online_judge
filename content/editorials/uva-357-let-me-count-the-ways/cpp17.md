ways[0]=1 是唯一初始方案，代表沒有選硬幣。coin 迴圈在外，amount 從 coin 遞增到上限；這個迴圈順序同時決定「不分排列」與「每種可用無限次」，不能隨意交換或反轉。

所有查詢共用一次預算結果，輸入順序即使重複、遞減或包含零都可直接查表。輸出分支依 ways[amount] 是否為一選擇完整句型，不是依 amount 是否為一；例如四美分與零美分同樣只有一種。使用無號 64 位元計數，輸出時不轉回 int。
