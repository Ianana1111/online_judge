The `days` map is rebuilt per case. `find` returns an iterator without insertion, and comparison with `end` distinguishes an unknown subject.

The first condition short-circuits, so it never dereferences a missing iterator. It also groups unknown and beyond-grace outcomes. The next branch handles on-time completion, leaving exactly the accepted-late interval. One output template preserves the case label and message punctuation.
