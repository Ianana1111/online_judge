外層迴圈先完整讀入 n、q，再判斷兩者是否同時為零。排序後仍保留重複的彈珠，因此 `it - marbles.begin()` 算的是所有彈珠中的索引。

`lower_bound` 回傳第一個不小於 x 的 iterator。判斷式先檢查 `it != marbles.end()`，短路求值會保證只有有效位置才執行 `*it == x`，避免尾端或空陣列的越界。找到時加一輸出一開始的排名，找不到則照格式印 not found。每組資料只印一次遞增的 CASE 標頭，接著讀完該組所有查詢，再處理下一組。
