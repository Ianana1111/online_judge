coverage 的 dp 上限值 limit+1 只表示超過可用張數，無需計算任意大的最少張數。金額迴圈內遍歷全部面額後才判缺口，保證尚未看完的面額不會被漏掉。最大金額由 limit*stamps.back() 給出，沒有魔法常數截斷。

bestCoverage 從 −1 開始，所以第一套一定成為候選。覆蓋相同時，先比較 size，再用 rbegin/rend 作倒序字典比較。setw 只影響下一次輸出，因此每個面額前各設一次三格欄寬，覆蓋值前設四格；文字、冒號和欄位的空格都有固定來源。
