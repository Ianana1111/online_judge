外圈取 encoded[i++] 當作目前字母，並把 count 設為零。內圈首先檢查 i 沒有超過字串，再確認是數字，避免最後一段讀到越界位置。

每讀一位便把舊值乘十加上新位數，i 前進到下一個位置。內圈結束後 decoded.append(count,letter) 一次補入整段，不論結束原因是下一個字母還是 EOF 都會執行。最後依 Case 編號、冒號空格與結果的格式印一行，每組使用新的 decoded。
