兩個數量都是用 >> 讀整數，緊接著 ignore 到換行，避免下一次 getline 收到上一行剩餘內容。讀取每組時先取 first，再取 second，存成一對映射。

行尾 CR 只在確實存在時移除，其他空格原樣保留。查詢用 responses.at(first)，表達題目保證查得到，不會在未知鍵時偷偷建立空字串。輸出直接寫完整下句並換行，因而保留詞序與空格，也不受上句是否含多個單字影響。
