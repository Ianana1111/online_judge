press 是當前列的按鈕集合，不是目前亮燈集合；previous 保存前一列按鈕。next 由原燈與所有已知切換效果 XOR 得到，恰是下一列為了清空本列必須按的位置。更新 previous 前先算好 next，避免混用兩列狀態。

每輪先計 count，再推導下一列，因此第一列與最後一列都計入。跑完十列後的 press 代表不存在的第十一列需求，只有零才可行。answer 初始101代表未找到可行解，最後依題目格式轉成 −1。
