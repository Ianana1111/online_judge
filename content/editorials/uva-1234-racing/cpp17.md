edges 把 cost 放在 tuple 第一欄，反向排序就會先處理昂貴道路。unite 成功表示可將道路保留而不產生環；失敗才把費用加到 answer。並查集使用路徑壓縮及按大小合併，確保大量邊時仍能快速判斷連通性。

讀取首行 cases 後精確處理指定組數；檔案最後的零是結束標記，不是額外圖形。每組都新建並查集與答案，樹狀圖所有邊都合併成功，自然輸出零。相同費用的排序次序只會影響保留哪一棵樹，不影響答案。
