每個 top 都建立全零 columns，再隨 bottom 遞增累加一整列，所以 columns 始終對應目前上下邊界。ending 直接從第一欄值開始，保證子段非空；之後每一欄都比較重新開始與接續。

answer 初始化為 INT_MIN，連全負矩陣也能正確更新。每組矩陣獨立配置與計算。while(cin>>n&&n) 同時支援題面指定的零終止與正常 EOF，不會讀取不存在的下一個矩陣，也不對 n=0 建立空陣列後索引第一欄。
