signature 接收字串副本，因此排序不會改壞 words 保存的原文。keys 與 words 使用同一個索引，匹配時取回原始單字。answer 每個查詢重新建立，所以項目編號自然重設。

setw(3) 只作用在下一個輸出的整數，產生範例要求的三字元欄位；後面補右括號與一個空格。只有 dataset 之間由 test 印空白行，同一組不同查詢之間直接接續標頭。cin 會跳過輸入中的分隔空行，END 判斷發生在產生任何輸出之前。
