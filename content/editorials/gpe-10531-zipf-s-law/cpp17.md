標頭以 string 讀入，再逐位累加並封頂，任何超過一萬的要求都保持為不可能的頻率值。內部 getline 先比較整行是否完全等於 EndOfText，其他空行仍可作合法文字內容。

finish 小函式只在 word 非空時累加，隨後清空；它同時處理標點、數字與行尾，所以切字規則一致。字母判斷明確限制 ASCII 英文大小寫，轉換後才存入 map。found 記錄是否有符合頻率的詞，避免印完結果又多印缺省句；first 則只負責案例間的空白行。
