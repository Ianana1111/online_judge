`dr`、`dc` 配對表示上、下、左、右四個方向。每一天先用 `next = grid` 保留沒有遭到入侵的格子，再依目前種類求出唯一的天敵：R 對應 P、P 對應 S、S 對應 R。

鄰居索引先通過上下界檢查，才讀取 `grid[nr][nc]`，避免在邊界越界。即使找到多個敵方鄰居，也只會重複把同一格設為相同種類，結果一致。內層迴圈不改 grid，因此整天的比較都是基於舊棋盤；`grid.swap(next)` 只在整天結束後執行。最後以測試案例索引決定是否先印一個空行，逐列輸出棋盤，零天的案例自然保留初始值。
