主迴圈用 long long 讀取差值，讀到零時直接結束，不輸出終止資料。q 和 r 分別記錄除以九的商與餘數，因此完全不需要浮點數或試誤搜尋。

`r == 0` 分支對應尾數九與尾數零的兩個合法原數。程式先印較小的 `10*q-1`，再印 `10*q`，中間只有一個空格。另一分支直接還原十位以上的 M=q 和尾數 r。數值上界保證乘十仍在 long long 範圍中，字面常數十會與 q 一起以較寬型別運算，最後每筆輸出一個換行。
