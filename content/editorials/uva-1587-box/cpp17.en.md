The first face is read in the EOF-controlled loop and the next five complete the case. `minmax` gives every rectangle a consistent orientation, and pair sorting orders by short then long side.

`pairs` verifies opposite-face duplicates. `edges` checks the repeated x, y, and z coordinates across the three representative face types. Both are required, while equality comparisons deliberately permit boxes with two or three equal edge lengths.
