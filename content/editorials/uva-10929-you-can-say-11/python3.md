輸入的數字可能長達一千位，不能直接轉成一般整數；而且輸出要保留原本的字串。從左到右處理每個數位，若目前前綴除以十一的餘數是 r，加入下一位 d 後的新餘數就是 `(r × 10 + d) % 11`。每一步的 r 都只在零到十之間，所以數字再長也不會溢位。最後餘數為零才是十一的倍數，依題目輸出完整原字串及固定英文句子。單獨的字串 `0` 是終止標記，不應輸出。

這份 python3 程式將上述推導直接寫成迴圈或公式，並保留題目要求的輸入結束與輸出格式。
