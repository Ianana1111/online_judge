A number may contain one thousand digits, so it cannot be parsed into an ordinary integer; its original spelling must also be preserved for output. Scan digits left to right. If the current prefix has remainder r modulo 11 and the next digit is d, update to `(r * 10 + d) % 11`. The remainder always stays between zero and ten, regardless of input length. A final zero remainder means a multiple of 11. Print the original token with the exact required sentence. The single token `0` terminates input without output.

The python3 program implements this reasoning directly and follows the exact input terminator and output format.
