每組先印 N、M，再判斷是否為終止行，因此最後的 `0 0` 不會漏掉。終止判斷也在建立比較函式與取餘數之前，避免以 M=0 做除法。

數字儲存在 `long long`，能完整保存所有有號 32 位元輸入，包括 −2147483648。比較函式用 `[mod]` 擷取當組的 M，餘數不同時立即返回；餘數相同才判斷奇偶。

`oddA` 為真表示 a 是奇數，所以兩數奇偶不同時直接回傳 `oddA`，即可讓奇數優先。最後奇數使用 `a>b`，偶數使用 `a<b`。同值比較自然為 false，不需要對重複數字去重。
