`remaining` stores unfactored parts and counts begin at multiplicative identity one. Ceiling division finds the first interval multiple, including low itself when divisible.

Prime-square comparison is widened before multiplication. Each repeated division determines exponent, followed by one `(exponent+1)` factor. Residual factors are applied before comparison, and ascending strict updates preserve the smallest tie. Original endpoints are retained for exact output formatting.
