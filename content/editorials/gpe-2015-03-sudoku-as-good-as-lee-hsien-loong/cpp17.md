row、column、box 的第 d−1 位代表數字 d 已使用；宮編號由 r/3*3+c/3 得到。empty 只收集原本的零，因此搜索不會觸碰已知線索。讀完整盤面後才開始求解，即使先發現矛盾也仍消耗全部輸入。

search 每層從尚未填的格子中找最少候選，將選中格交換到當前位置，便能用 at 表示已完成前綴。options 的最低一位選出一個候選，ctz 轉回數字。遞迴失敗時清除三個遮罩位元並把格子歸零，最後也交換回 empty 順序；成功則保留完成盤面。輸出九行九數，無解只印 NO，不加入額外解說。
