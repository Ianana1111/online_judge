`Maker` keeps a name and its two endpoints together. The range loop always visits all makers; `answer` may be overwritten, but it is used only if the final match count is one.

`matches` and `answer` are declared inside each query, preventing stale results. The test-case index inserts exactly one blank line before every case after the first, while query lines remain consecutive.
