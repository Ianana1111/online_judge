這題要對同一串字元做兩次不同的解讀，不是把十進位數值換個顯示方式。保留輸入字串，逐位將 `decimal` 乘十再加入數位，同時將 `hexadecimal` 乘十六再加入同一數位；例如字串 `10` 得到十與十六兩個值。接著用除二取餘數反覆檢查最低位，把二進位中為一的位元數出來。數字只有一到四位，兩種解讀都能放入一般整數。先讀組數，每組輸出兩個計數，勿輸出題目故事中的 XOR 值。

python3 程式把這個推理直接轉成計算或查表，並依題目規定讀取每一組資料與輸出答案。
