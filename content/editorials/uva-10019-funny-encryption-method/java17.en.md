The same characters must be interpreted twice; this is not merely printing a decimal value in another notation. Keep the token as text. For each digit, multiply the decimal accumulator by ten and the hexadecimal accumulator by sixteen, then add that same digit to both. For instance, the text `10` represents ten and sixteen. Next, count binary one-bits by repeatedly taking the remainder modulo two and dividing by two. Inputs contain at most four digits, so ordinary integers suffice. Read the case count and print only the two bit counts, not the story’s XOR result.

The java17 program turns this reasoning into direct computation or lookup while preserving the required input and output format.
