題目要反轉的是每一組內部，而不是整個字串。若字串長度為 L、組數為 G，則每組大小固定為 L/G。依序取出從 0、L/G、2L/G 等位置開始的片段，各自反轉後按原組序接回去。組與組之間的先後不變，所以把整串一起反轉會得到不同答案。輸入的組數為零表示結束，不要把它當成要計算的案例。每個字元只被反轉一次，總時間 O(L)。

每組先 `substring`，再以 `StringBuilder.reverse()` 反轉該組。
