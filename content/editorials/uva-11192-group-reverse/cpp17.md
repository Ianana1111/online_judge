while 的條件先讀 groups，再確認非零，所以終止行不會進入本文。接著讀 text，計算 size=text.size()/groups，合法輸入保證 size 為正且無餘數。

start 每次增加 size，恰好依序走訪每組的起點。reverse 接收左閉右開迭代器範圍，第二個迭代器必須是 start+size，不能再加一。因為所有區段不重疊，之前反轉過的內容不會被後續操作影響。最後只輸出修改後的整串與換行，不加組界符號或案例標頭。
