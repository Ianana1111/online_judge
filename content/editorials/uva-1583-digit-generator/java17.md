目標 M 的生成元 N 必定不大於 M，因為 N 還要加上非負的數位和。所有查詢最多十萬，所以先由小到大枚舉每個 N，算出 `target=N+digitSum(N)`，再記錄它。對同一目標只在表格原本為零時寫入：第一個碰到的 N 就是最小生成元，後面較大的不能覆蓋。表格保留零表示找不到生成元，和題目輸出規則一致。處理輸入查詢時只需查表，不必對每個 M 重新搜尋。

java17 程式依上述不變量更新資料；每個分支都對應一種明確的輸入或狀態。
