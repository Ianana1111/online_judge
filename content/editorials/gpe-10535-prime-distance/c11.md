本站這題不是找相鄰質數，而是把相同硬幣放到一排格子，要求任意兩個有放硬幣的格子距離都是質數。先數「哪些格子非空」的合法形狀，再數每個非空格子至少一枚的分配方式；兩件事分開後，M 枚硬幣放到 s 個指定非空格子的方式就是 C(M−1,s−1)。

合法形狀最多四格：一格有 N 種位置；兩格對每個質數距離 p 有 N−p 種平移；三格來自孿生質數結構，有兩個鏡像；四格只剩距離結構 0、2、5、7 的平移。用篩法預存質數與孿生質數的個數和總和，就能 O(1) 算出每種形狀的平移總數。最後只需 s=1..4 的四個組合數，模數下除以 2、6 用模逆元，不能直接做一般整除。
