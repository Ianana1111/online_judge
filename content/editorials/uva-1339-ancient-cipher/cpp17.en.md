Fresh zeroed arrays count A through Z for each pair. Sorting both arrays erases only which letter owns a count.

`array` equality compares all 26 entries, including duplicate counts and zeroes, unlike a set. Only complete equality prints `YES`. The outer loop reads exactly two strings per case until EOF, with no test count.
