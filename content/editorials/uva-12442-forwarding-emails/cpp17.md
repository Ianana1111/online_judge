next 依輸入的 u 存入，不依賴各行順序。removed 保存實際剝除順序，之後 rbegin 到 rend 讓每個點的後繼先完成。indegree 在剝除後大於零者才需要環處理。

找到新環時先收集 cycle，再用 cycle.size() 統一賦值，避免只給部分節點或把環當長度一。reach[start]==0 可略過已處理過的同一環。best 從零基底編號零開始，嚴格比較保留同分最小編號，最後加一輸出題目的編號。
