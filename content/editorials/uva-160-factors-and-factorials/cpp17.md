composite 初始全 false，遇到未標記的 p 就保存，並從 p*p 開始標記倍數。每筆 N 的 quotient 從 N/p 開始，不斷整數除以 p，所以依序就是各次方的倍數數量，沒有乘法溢位風險。

setw(3) 只影響下一個輸出欄位，因此 N 與每個 exponent 都各自設定。第一行前綴是寬三的 N 加上 ! =，共六字元；續行用六空格對齊。column 達十五時先換行再印下一數，最後統一補一個換行，不多插案例空白行。
