讀完 n 後 ignore 到換行，讓第一次 getline 正確取得第一筆紀錄。每列建立獨立的 istringstream，只讀 country 就足夠；姓名保留在這一列的串流中，不會流到下一列。

`++counts[country]` 在國家第一次出現時會先建立零值再加一，之後只更新同一筆鍵。std::map 使用字串的字典順序管理鍵，所以最後的範圍迴圈已符合排序需求。每個國家只印國名、單一空格與計數，不輸出姓名或額外案例標頭。
