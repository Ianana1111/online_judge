`best=None` represents no legal interval yet. V and H reset for every start and accumulate the whole slice; exceeding capacity breaks, while being short of half merely continues because later jars may help.

`error` is the numerator of temperature difference. Saving it with its volume enables exact cross comparison, whose strict inequality preserves earlier ties. Early return occurs only for error zero. All jars are read before `solve`, so returning early cannot leave tokens that corrupt the next case.
