ids 將所有插座、設備及轉接器涉及的字串統一映射成頂點，包含只出現在轉接鏈中間的類型。先讀完取得完整頂點數，再建立流網路，避免陣列大小不足。設备名稱只用於讀取，真正決定相容的是第二個字串。

Dinic 的 add 同時加入反向殘餘邊，並處理自環時的反向索引；BFS 建層、send 沿層級遞增邊推送，反向邊可撤回先前選擇而找到更好的配對。來源和匯點邊保存一對一限制，只有轉接邊容量設 m。最後按題目要求在案例之間印空白行。
