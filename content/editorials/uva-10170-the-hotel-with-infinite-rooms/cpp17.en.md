The `through` lambda captures the current starting size and evaluates the exact arithmetic-series sum. Both factors are already `long long`, so multiplication does not occur in a narrow type.

A qualifying midpoint remains as `high` because an earlier group may also qualify; an insufficient midpoint advances `low` to `mid+1`. The lower bound begins at S. When `D<=S`, search correctly returns the first group because all those days lie within it.
