All four inputs are `long long`, so cross products are wide from their first operation. The all-zero sentinel is tested before division or gcd, preventing a zero denominator.

The unreduced numerator and denominator are divided by the same gcd. The denominator is printed only when it differs from one, without spaces around the slash. Case numbering advances only for actual cases, and both required output lines retain exact punctuation.
