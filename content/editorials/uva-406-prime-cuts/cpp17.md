質數表仍把一標成 false，避免把本題特例混進一般質數判定。每組以 `values{1}` 明確加入題目要求的一，之後才掃描真正質數，所以清單保持遞增而且沒有重複。

`length % 2` 在奇數長度時為一，讓 take 自動少取一個；min 保證不會超過清單大小。start 左右對稱，輸出迴圈使用半開區間 `[start,start+take)`，恰好印出所需個數。每個元素前先印一個空格，冒號後不另加多餘空格；兩個換行提供案例之間的空白行。
