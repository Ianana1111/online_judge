外層 `while (cin >> n)` 讓每一組資料獨立處理，EOF 自然結束迴圈。陣列 a 保留原本的相對順序，不呼叫 sort，因為排序後再計數會把所有逆序都消掉。

索引 i 從頭走到尾，j 從 i+1 開始，確保每對不同位置只被檢查一次。條件 `a[i] > a[j]` 的嚴格大於會排除相等元素；符合條件時才增加 inversions。每組讀取後重新設為零，避免累計到下一組。最終直接輸出總數，不需要真的執行交換；計數正確性來自相鄰交換與逆序對數之間的一對一減少關係。
