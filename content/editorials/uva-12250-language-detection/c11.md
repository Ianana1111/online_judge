題目只認得六個完整的招呼語，所以直接建立「字串到語言」的對照表最清楚。讀入一個單字後，必須完全相等才查到語言；例如 `HELLOO` 不能因為像 `HELLO` 就猜成英文，必須輸出 `UNKNOWN`。`HELLO` 和 `HALLO` 也要分清楚。每處理一個有效單字才增加案例編號，依指定格式輸出 `Case 編號: 語言`。單獨的 `#` 是結束標記，不編號、不輸出；其他未知單字則仍是一筆案例。

c11 程式把這個推理直接轉成計算或查表，並依題目規定讀取每一組資料與輸出答案。
