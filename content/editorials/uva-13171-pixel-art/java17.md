每一種畫面顏色都能拆成品紅 M、黃色 Y、青色 C 三種原色。用三個位元標記需要哪些原色：R 用 M、Y，G 用 Y、C，V 用 M、C，黑色 B 用三種，白色 W 不消耗。先統計整張圖每種原色的總需求，再與三個庫存比較，避免遇到缺料後就提前離開，卻沒有讀完這一組圖案。

需求最多是像素個數，但初始庫存沒有明訂小上限。因此 C／C++ 以十進位字串保存庫存，將整數需求轉成字串後做精確比較與減法；Java 使用 BigInteger。任何一種庫存不足就只印 NO，全部足夠才印 YES 與 M、Y、C 順序的剩餘量。先按原色而不是按顏色扣料，是避免複合色遺漏的關鍵。
