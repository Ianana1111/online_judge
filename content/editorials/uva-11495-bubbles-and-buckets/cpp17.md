bit 保存已讀元素的出現次數，query(x) 包含所有≤x 的元素；排列互異，所以沒有與當前 x 相等的先前元素。由已讀總數 i 扣去此值，便是新增逆序數。

moves 使用 long long，而單次 Fenwick 計數至多十萬，bit 仍可用 int。更新從 x 起沿父區間增加一，查詢則沿低位元向左累加。此題數值本來就是一至 N，不需座標壓縮；輸出在勝者後補上精確 moves，遵守本站格式。
