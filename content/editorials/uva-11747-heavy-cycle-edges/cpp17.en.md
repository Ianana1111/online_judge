Edges are stored as `(weight,u,v)`, so default tuple sorting orders primarily by weight. `unite` returns false exactly when the endpoints already share a component, which is the condition for recording a heavy cycle edge.

Path compression and union by size change only efficiency. Recorded weights are already increasing from the scan. An empty list prints lowercase `forest`; otherwise one space separates values. Every dataset receives a fresh DSU.
