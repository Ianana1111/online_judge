edges 以 tuple(weight,u,v) 保存，所以預設排序首先按權重。DSU 的 unite 回傳是否真的合併：false 表示兩端原本就連通，對應本題需要列出的邊。路徑壓縮與按集合大小合併只提升效率，不改變分量的意義。

heavy 按掃描顺序收集，無須第二次排序。empty 表示沒有任何環，輸出固定小寫 forest；否則相鄰權重之間只加一個空格。每組都重新建立 DSU，n>0、m=0 時仍正常輸出，只有 n=m=0 才停止。
