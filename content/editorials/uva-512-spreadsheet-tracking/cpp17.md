position 的索引永久使用原始 cols，因此查詢 (r,c) 能直接找到那份原始資料。各操作修改的是索引所保存的目前座標，不能改變索引本身。消失用 (-1,-1) 表示，合法 EX 座標皆為正，不會與消失資料碰撞。

EX 的 if/else if 確保每筆資料只交換一次。批次指令先用 before 保存對應維度，直接刪除優先處理；其餘才累計 shift。最後根據 R 或 C 更新一個維度，另一維度不動。caseNo 控制試算表間空行，輸出始終同時列出查詢的原始座標與目前座標。
