best[0] 為零；每輪的 u 使用線性掃描找最小未確定值。先標記 used，再檢查 u 是否為目標，因為只有取出那一刻的值已經確定。更新式 min(best[v],max(...)) 直接對應「在多條路徑間取較小，在單一路徑內取最大」。

所有距離比較使用整數，sqrtl 只在輸出時求值。scenario 跨案例遞增，但 best 與 used 每組重新建立。輸出兩次換行保留案例間空白；公開範例與 Submit 共用同一 checker，會驗證標頭、行數和數值。
