The first three letters form a base-26 number with A=0 and Z=25. Reading left to right, multiply the current value by 26 and add the next letter’s offset from A. This avoids manually writing positional powers. Interpret the four characters after the hyphen as a decimal number; leading zeros do not change its value. Compare the absolute difference with 100, including equality: a difference of exactly 100 is `nice`. Read the test count and print one answer for each plate.

The python3 program updates the state using this invariant and handles every input branch explicitly.
