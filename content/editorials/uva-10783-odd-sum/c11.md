先把問題換成「從一到界線的奇數總和」。不超過 n 的正奇數有 `floor((n+1)/2)` 個，而前 k 個正奇數 `1+3+…+(2k−1)` 的和是 `k²`。所以 `odd_prefix(n)` 先數出 k，再回傳平方。閉區間 `[a,b]` 的答案是 `odd_prefix(b)−odd_prefix(a−1)`，扣掉嚴格小於 a 的部分，保留兩端。當 a=0 時，`odd_prefix(-1)=0`，仍然正確。輸出案例編號從一開始，格式中的空白與冒號都要照題意。

c11 程式把這個推理直接轉成計算或查表，並依題目規定讀取每一組資料與輸出答案。
