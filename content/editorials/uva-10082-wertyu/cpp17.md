`rows` 分別保存四排鍵盤，第二排結尾的反斜線在 C++ 字串中必須寫成 `\\`。建立映射的索引從 1 開始，所以不會將某排第一個按鍵錯誤接到上一排的最後一個按鍵。

decoded 初始為恆等映射，再針對合法鍵盤字元覆寫成左鄰鍵。索引轉成 unsigned char，確保不會用負值存取陣列；輸入保證合法，預設映射主要用來保留空格。

getline 讀完整一行，包括行首及連續空格。若是 CRLF 輸入，移除行尾的回車符後再輸出一個換行；每個字元只查表一次，能完整保留多行訊息。
