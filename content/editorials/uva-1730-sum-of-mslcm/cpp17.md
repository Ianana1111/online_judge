left 指向尚未處理的最小因數，quotient 是當前商，right=n/quotient 一次找出相同商區間的尾端。等差總和先用 long long 相乘，再除二，避免在整數除法前錯把單一因子除二而截斷。

每次把 left 移到 right+1，確保進度向前且分段恰好銜接。迴圈計算的是一到 n 的 sigma 總和，所以輸出 answer−1 才符合從二開始的題意。每個查詢從零累計，輸入零時直接離開，不輸出額外一行。
