外層 `while` 只負責輸入初值與終止符；內層迴圈沒有 `value != 0` 條件，因此可以正確記錄運算後到達的零。每組重新初始化 seen，避免不同起點共享錯誤的造訪紀錄。

迴圈先把當前值設為已見，再增加 count，最後才更新到下一項，確保初值被計入且重複的那次不會多算。整數式 `(value*value/100)%10000` 與八位補零後切中間四位等價，所有狀態都留在陣列合法索引範圍內。
