Only the outer input loop interprets zero as a sentinel. The inner loop permits and records generated zero normally, and each case receives a freshly cleared state array.

The current value is marked and counted before transition, ensuring the initial state is included and the repeated occurrence is not. `(value*value/100)%10000` keeps every result within valid array indices and is equivalent to padded decimal slicing.
