以二維 `char` 陣列表示目前圖片，按行解析命令。`I` 重建、`C` 清成白色但保留尺寸；`L`、`V`、`H`、`K` 逐格改色。座標先 x 後 y，兩者皆從一開始，存取陣列時要換成 `image[y-1][x-1]`。

`F` 是唯一需要搜尋的命令：若原色與目標色不同，就從起點做四方向 BFS，只擴展原色像素，入隊時立刻染色。用整數陣列當佇列，容量為圖片像素數；即使整圖連通也不會遞迴溢位。
