Replacing commas with spaces lets tokenization handle matrix formatting uniformly. Edge extraction uses `a<b` and positive weights, then tuple sorting orders by weight and endpoints.

DSU uses path halving and union by size. `chosen` stores original city endpoints rather than component representatives, so printed letters match actual tunnels. The loop stops at `n-1`; the semantic checker accepts any real, connected, acyclic minimum tree and edge order.
