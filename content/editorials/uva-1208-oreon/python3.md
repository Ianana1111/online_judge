先把輸入中的逗號轉成空白，再用 split 讀數字，逗號旁空格數不會影響解析。edges 僅含 a<b 且權重正的資料，排序鍵為 weight、a、b。find 使用路徑壓縮，size 決定把較小的樹接到較大的樹上。

chosen 記錄原始邊端點，不是合併後的群代表，因此輸出的城市字母正確。已同群時 continue，不會重複計入任何環邊。每組印 Case 編號及 N−1 條實際隧道；語意 checker 會核對輸入邊、權重、無環連通及最小總權重，允許不同合法生成樹與不同列出順序。
