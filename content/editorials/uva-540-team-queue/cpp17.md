owner 是直接編號映射，當組的全部合法成員都在讀隊伍時重新寫入，因此不必為每組清除整個一百萬格陣列。members 與 active 則每組新建，沒有排隊狀態殘留。

只有在 members[team].empty() 時新增 active 項目，確保同隊只有一個區塊。DEQUEUE 依題目有效命令對非空隊列操作，先輸出、移除成員，再判斷是否刪除隊伍。每組標頭及最後空白行都按格式輸出，即使當組沒有任何出隊也一樣。
