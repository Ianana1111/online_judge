PairSum 同時保留總和與原排序陣列的兩個索引，不能只留數值和。建表使用 i<j，保證配對自身兩元素不同且不重複建立反向配對。

lower_bound 使用與排序一致的 sum 比較，得到首個不小於 target 的位置，再只檢查等於 target 的連續區段。四個索引不重複才設 found；found 與 answer 分開，因此合法零或負答案不會被當成沒找到。d 從陣列尾端往前走，命中後停止所有查詢。
