Edge 保存目的點、反向邊所在索引、目前殘量。新增無向邊時兩端都從容量一開始，它們互為反向邊；增廣後其中一端可能變二，表示除了原可用方向，還能撤回之前的反向使用，並非允許兩條路重複使用同一邊。

parent 同時表示 BFS 是否到過與前一點，edge 記錄從前一點使用哪個 adjacency 索引。找到 target 後反向追溯整條增廣路，更新成對容量。每輪重新建立 BFS 狀態，殘量容量則保留第一輪的改動。兩輪即停止，最後按 Case 編號輸出 YES 或 NO。
