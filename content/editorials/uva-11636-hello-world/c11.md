一開始已經有一行，因此 N=1 時不必貼上。一次貼上最多能複製當前的全部行數，所以做一次後可達到的行數上限翻倍；如果連這個上限都不到 N，任何方法也做不到。反覆讓 `capacity` 翻倍，直到第一次達到或超過 N，次數就是答案。最後一步可以只貼缺少的行，所以超過 N 的容量不代表真的貼過頭。輸入遇到非正數才結束，案例編號只對已輸出的正數增加。

C 版本以 `scanf` 讀取完整資料，使用題目範圍內足夠的整數型別；每筆答案用 `printf` 單獨輸出。
