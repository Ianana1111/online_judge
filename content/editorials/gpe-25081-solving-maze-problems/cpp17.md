grid 保存原始字元，parent 初始 −1。先掃描找到兩個端點，將起點 parent 設為自身後入列，四個方向依序檢查邊界、障礙與是否已訪問。走訪時只設定 parent，不改 grid。

搜尋結束後先檢查 goal 是否被發現；無解立即輸出固定文字。成功時從 goal 開始改成 +，直到 start 也改完才 break，避免漏掉任一端點。最後按原來十列輸出，障礙和不在解答上的開放格子保持原樣。
