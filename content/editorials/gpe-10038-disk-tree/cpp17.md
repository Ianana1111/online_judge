nodes[0] 是不顯示的根，children 保存孩子名稱與索引。新增前先記住 next 並寫入父節點，再 push_back；下一次取父節點時重新用索引存取，沒有保留可能因 vector 擴張而失效的參照。

printTree 的 depth 是即將印出的孩子縮排量，根以零呼叫，所以最上層不多一格。map 的遍歷順序已符合名稱字典排序，不需要額外排序陣列。讀取路徑使用串流單字讀法是安全的，因為題面明確保證路徑內沒有空格。
