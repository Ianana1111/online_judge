d 最初完整讀取每條有向邊，不將兩個方向取最小，也不預跑全圖 Floyd。order 保存正向拆除順序，因此 step 從 n−1 往零走，active 是這輪對應仍存在的塔。

每輪以最新的 k 當唯一新增中繼，u、v 都遍歷零到 n−1。這讓日後才啟用的端點也累積既有中繼點帶來的改善，是這種寫法能直接加入新點的關鍵。加總時才改遍歷 active；u=v 的零距離不影響結果，所以无需額外排除。answer 在所有反向階段持續累加，最後每組只輸出一個整數。
