arrive 先排除不可行的 -1，再計算抵達時間並與 deadline 嚴格比較。earliest 專門處理兩個候選之一或兩者不可行的情況，避免把 -1 選成更好的時間。

固定 length 時，left[l]、right[l] 起初代表上一個長度的 [l,l+length−2]，索引 l+1 則代表 [l+1,r]。nextLeft、nextRight 都先算好，才覆寫索引 l。l 由小到大掃，尚未使用的 l+1 不會被提前覆寫，因此兩個一維陣列就足夠。初始每個單點均可能作為起點，最後只需讀完整區間的索引零。
