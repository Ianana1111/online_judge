i、j 初始為零與一，k 從零開始。比較使用取模索引，所以跨到字串末端時會回到開頭，與項鍊定義一致。當字元相同只推進 k，真正失配才移動較差候選並把 k 重設零。

跳過 k+1 個起點是演算法效率的關鍵，不是只往後挪一格。移動後若 i==j，就再跳過這個已由另一指標代表的位置。迴圈也在 k==n 時停止，避免全相同或週期字串無限比較；最後 min(i,j)+1 同時完成最早同分起點與一基編號轉換。
