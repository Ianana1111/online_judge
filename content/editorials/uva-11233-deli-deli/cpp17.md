`endsWith` 先確認單字長度不小於後綴，再用 `compare` 比較最後一段，因此單字只有一個字元時也能安全檢查 ch、sh。

`irregular` 的鍵是單數，值是指定複數。查詢使用 `find`，不會因為查不到而新增空字串項目。只有查不到不規則單字時，才繼續判斷字尾。

子音加 y 的條件先檢查長度，再讀倒數第二字元；`vowels.find(...) == string::npos` 表示它不在五個母音中。符合時用 `substr` 去掉最後的 y，再印 ies。剩下五種後綴共用接 es 的分支，最後的預設分支接 s，每筆查詢統一換行。
