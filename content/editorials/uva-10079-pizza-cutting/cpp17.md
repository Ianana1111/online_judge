cuts 從宣告起就是 long long，所以 `cuts*(cuts+1)` 會以 64 位元計算，不會先在 int 中溢位。連續兩個整數的乘積一定為偶數，因此整數除以 2 可以精確得到等差級數總和。

輸入迴圈判斷 `cuts>=0`，讓 0 仍被正常處理，所有負數則直接結束。公式開頭的 1 代表尚未切割時原本就存在的一整塊披薩。

每次只輸出 pieces 與換行，沒有測試編號、說明文字或額外空白，也不需保存前一筆結果或事先建立直到最大 N 的陣列。
