先從增加一刀來想：第 k 條直線最多與前面 k−1 條線相交，於是被分成 k 段，最多讓塊數增加 k。從零刀的一塊開始，累加 1 到 N，得到 `1 + N(N+1)/2`。程式直接計算公式，不模擬切線；零刀必須輸出一。讀到第一個負數才結束，負數本身不輸出。因為乘積可能超過 32 位元，計算時就要用足夠大的整數型別。

Python 版本依空白拆開輸入，再按照每組的欄位數前進；整數運算保留精確結果。
