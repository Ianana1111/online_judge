第一輪掃描只數字典事件，不把符號或空白算進 W。字母段與數字段分開讀到結束，因此多位數索引不會被拆開。front 從 W+1 開始，每次前減一即可得到未使用且比舊位置更小的索引。

Fenwick::kth 用二進位躍升尋找累計有效位置首次達到 k 的地方；只計值為一的位置，已移走的洞不會占字典名次。取出 words[old] 後把舊計數減一，再儲存到新位置。非事件字元走 continue 分支直接輸出，因此連續空白與段落換行皆保留。
