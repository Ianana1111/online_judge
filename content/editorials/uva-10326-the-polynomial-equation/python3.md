coefficients 由低次到高次排列，使兩個更新位置恰是 degree 與 degree+1。updated 每次長度多一，並在整個因式計算完才取代舊陣列，保證各項貢獻取自相同舊多項式。

格式化迴圈反向走次方；degree>0 的零項跳過，常數零不跳。term 只含絕對值與變數部分，正負由 terms 是否已有項目與 coefficient 的符號決定。最高次恆為正一，所以首項可直接輸出；最後统一附上等號與零。
