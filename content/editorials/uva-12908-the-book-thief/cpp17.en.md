The search indices and product are `long long`. A midpoint whose triangular number is strictly greater may be the answer, so it moves `high`; otherwise the answer must start at `mid+1`.

At convergence, `low` is the first valid page count. The code obtains `missing` by exact triangular subtraction and prints it first. The zero sentinel produces no case line or label.
