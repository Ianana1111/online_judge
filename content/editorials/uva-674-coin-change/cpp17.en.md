`ways[0]=1` represents the unique empty selection. The denomination loop is outside and the amount loop increases, the exact ordering needed for unordered combinations with unlimited copies.

One table serves repeated, decreasing, and zero-valued queries without mutation. Output contains only `ways[amount]` and a newline. `LIMIT` is exactly 7489 and the vector includes index zero, so both range endpoints are valid.
