ways[0]=1 是唯一初始方案，代表沒有選硬幣。coin 迴圈在外，amount 從 coin 遞增到上限；這個迴圈順序同時決定「不分排列」與「每種可用無限次」，不能隨意交換或反轉。

所有查詢共用一次預算結果，輸入順序即使重複、遞減或包含零都可直接查表。每次只印出 ways[amount] 和換行，不加額外文字。LIMIT 精確對應 7489，向量多配置索引零，因此最大合法金額與零元都能安全存取。
