主程式先讀字典大小與描述數，再每兩個 token 讀一組單字金額。Fraction(amount) 接受有限十進位與指數記法，沒有經過 float。內層共用同一個輸入迭代器，遇到句點就跳出，下一份描述從正確位置繼續。

decimal_string 分別計算既約分母中的二與五次方，取較大次方 places，補成 10^places 後得到精確的整數刻度。固定 places 補零確保很小的小數也有正確的前導零，最後只移除小數末端無意義的零。所有金額非負，薪資自然非負；零的分母是一，直接輸出 0。
