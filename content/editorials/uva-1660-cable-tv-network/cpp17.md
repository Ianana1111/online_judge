輸入邊的括號與逗號分別以 char 讀掉，節點編號填入布林鄰接矩陣，因此重複列出的連線不會重複影響度數。n≤1 直接回傳 n，包含空網路的零，之後仍繼續讀到 EOF。

對非完全圖，最小度數是有效初始上界：刪除最小度節點的全部鄰居會留下它及至少一個不相鄰節點。flow 只需找到比目前上界更小的值；若已送滿 limit，提前返回也不會錯過改進。每次重新建 network，避免前一對端點的殘量容量污染下一對。DFS 只沿層次遞增邊，反向容量以 reverse 索引正確更新。
