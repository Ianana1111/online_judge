queries 保存 (N,原索引)，sort 讓 step 單調遞增，每輪只計算一次機率項與對數項。accumulate 將上一次加法被捨去的低位誤差保存在 compensation，下一次先扣除它，降低長序列的累加偏差。

k 明確轉為 double 再算 k·(k+1)，避免整數乘法溢位。概率由 −expm1(logSurvival) 還原，前導零由兩份階乘對數加 log10(N+1) 取 floor；target=0 直接回覆定義值。answer 依原索引存回結果，最後以六位小數和整數零個數逐行輸出。

實測使用 double 加補償加總，避免部分沙箱平台將 long double 函式實作為較慢的軟體高精度運算。獨立高精度驗算另檢查整數對數與小數捨入邊界，不能僅因輸出格式相同就認定誤差足夠小。
