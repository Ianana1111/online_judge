每一步抽到指定球的機率很小，直接反覆相乘後再以 1 減掉乘積，容易把微小差異消掉。先累加失敗機率的自然對數 `log1p(-1/(k(k+1)))`，最後以 `-expm1(logSurvival)` 求至少成功一次的機率，這兩個函式就是為接近零的量設計的。

全紅機率的分母是 N!·(N+1)!，前導零個數由 `floor(2·log10(N!)+log10(N+1))` 得到。把查詢依 N 排序，從目前最大已計算的 k 繼續累加，不為每個查詢重跑到一百萬。兩種累加都保留補償量，減少浮點加法誤差；最後按照原始索引輸出，N=0 要特別給 0.000000 和 0。
