pending 裡每筆帶 parent，樹沒有環，所以排除返回父村即可避免重走。每走一條路，把真正的 length 加到 distance，而非固定加一；best 只保存最遠村號及距離。

solve 把兩次搜尋包在一起，完成後清空 graph，因此空白行與 EOF 都能共用同一處處理。map 讓程式不必由道路行數猜最大村號或假定輸入順序。圖是雙向，讀一條道路必須在兩端的鄰接表各加入一次。

路長沒有指定上限，因此 Decimal 保存十進位整數，加法由個位數往前進位，比較則先看位數、再看字典序；不會把路徑總長截成 64 位元。
