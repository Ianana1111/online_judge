mask 的前兩位代表上、下邊，後兩位代表左、右邊。尺寸至少二，所以最多扣兩列或兩欄不會變負；二列全扣完時 cells=0，choose[0][0]=1 仍符合容斥所需的空選法。

先檢查 k>cells，便同時處理 K>400 的輸入，避免超出 choose 的欄索引。popcount 決定正負，answer 用 long long 累加十六項後再正規化。Pascal 表完全不依賴模除法，對此模數也安全。
