`sum` starts at zero and `largest` at -1. Each divisor loop begins with the divisor itself, ensuring that every number includes itself in its sum.

Only sums within 1000 index the reverse table. Increasing `n` makes assignment alone sufficient to retain the maximum. The case counter advances only for nonzero queries and output uses the required case format.
