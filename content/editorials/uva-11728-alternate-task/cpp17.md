sum 初始化零，largest 初始化 −1，分別代表尚未累計與尚無對應解。外圈 divisor 枚舉每個可能因數，內圈從 divisor 本身開始走倍數，所以會包含 n 自己這個因數。

建立反表時先檢查 sum[n]<=1000，避免超出查詢範圍。n 按遞增順序覆寫，因此無需額外呼叫 max。輸入 s 保證在合法範圍，除了零終止外都能直接查 largest[s]。tc 只在輸出時增加，格式使用 Case 編號、冒號與結果。
