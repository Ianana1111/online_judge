The program prints the header immediately after reading the case count. Both flags are reset for each ten-value array.

The loop begins with the second value and compares it to its predecessor. Flags only transition from true to false. The final OR accepts either strict direction, and output capitalization matches the required words.
