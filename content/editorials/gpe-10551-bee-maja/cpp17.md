coordinate 的 pair 預設為 (0,0)，因此中心一號已正確。step 同時更新位置與格號，只在 label≤limit 時寫入表，讓最後一圈走到終點也不會碰到非法索引。

dx、dy 對應六個蜂巢鄰接方向，並非平面上下左右。ring 外迴圈每次先走 (0,1)，所以第一方向的剩餘步數要少一；其餘方向完整走 ring 步。輸入只負責查表和印出 x、y，以空格分開，不加入括號或逗號。
