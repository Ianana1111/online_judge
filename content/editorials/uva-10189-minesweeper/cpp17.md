外層 `while` 一次讀取列數與欄數，遇到 `0 0` 就停止。`vector<string>` 保存每列字元，不必為盤面的最大尺寸建立全域陣列。

`field` 同時負責編號與空白行判斷：第一組的值為 0，不印分隔行；之後先換行，再輸出遞增後的編號。每一列答案輸出完成後再換行。

地雷分支的 `continue` 會跳到下一欄，安全格才執行八方向計數。C++ 的 `&&` 由左到右短路求值，因此範圍條件都成立後，才會存取 `grid[nr][nc]`。安全格最多相鄰八顆地雷，直接以整數輸出不會破壞每格一個字元的格式。
