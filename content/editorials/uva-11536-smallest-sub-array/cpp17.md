values 先寫入題面指定的三項，從索引三才使用遞迴公式。frequency 大小只需 k+1，判斷 value<=k 成立才存取；合法序列都為正數。++frequency[value]==1 表示新增一個必要種類，而 --frequency[removed]==0 才表示失去一個種類。

while 內先記錄當前合法長度，再左移；若移除不需要的數或仍有其他副本的值，視窗持續合法，可再縮短。best 用 n+1 作無解哨兵，輸出時比較 best>n，不會把合法長度 n 誤判。每組重新配置頻率及端點，避免案例間狀態殘留。
