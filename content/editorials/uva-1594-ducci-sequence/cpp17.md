all_of 要求每一格都為零，不能換成 any_of。seen 的元素是完整 vector，保留順序和每格值；insert 的 second 為 false 表示已存在。

next 的每一項完全由 state 計算，迴圈結束才 move 取代舊狀態，因此環狀最後一組差也讀到正確舊首項。seen 每組重新建立，不同初始向量的歷史不會混在一起。程式以真正的重複狀態證明循環，沒有隨意設定更短的步數當作失敗。
