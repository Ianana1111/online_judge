讀完 tests 後第一次 getline 只消耗該數字行剩餘內容，接下來每次 getline 都是一筆案例，包含空行。若輸入使用 CRLF，先移除行尾的 CR，避免把它當成括號。

右括號對應的 expected 是需要的左括號，判斷式先檢查 stack.empty，利用短路避免空容器存取。出錯時 valid=false，即使後面沒有繼續掃描也不會被當成成功。輸出條件同時要求 valid 與空堆疊，處理錯配與未閉合兩種情況。
