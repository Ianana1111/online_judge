rows 的鍵是高度，值是長 n 的空白字串；每個字元處理時只改自己 x 欄的位置。下降的 height 更新放在寫入之前，上升的更新放在寫入之後，持平完全不更新。

map 已依高度排序，begin 與 rbegin 取得最低與最高筆畫；輸出迴圈從高往低走，並只刪除每列最右側空格，不會刪掉必要的前導或內部空白。縱軸固定先印 | 再印空格，橫軸用 n+2 個減號。每組最後兩個換行保留題目要求的案例間空白。
