row[u][v] 表示輸入 E 是否存在 u 到 v 的邊，重複讀入同一關係仍只是同一布林值。bitset 的 & 計算共同後繼，any 判斷是否非空；只有此時才需要要求兩列完全相等。

每對只比較一次即可，因條件在 u、v 之間對稱。valid 即使變成 false 也不會再改回 true，輸入則在檢查前已完整讀完，下一組不會錯位。空圖的迴圈自然不執行，保留 true。輸出包含題面要求的 Case # 編號以及大小寫精確的 Yes 或 No。
