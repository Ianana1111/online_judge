dp 保存當前長度，next 每輪重新歸零，因此一條轉移只增加一位。full=(1<<base)-1 的每個位元都是一，只有此遮罩代表所有數位已出現。current 將不同尾數的完整數字相加，再和 answer[base][length-1] 合併成累計。

初始化迴圈從一開始，後續轉移仍可接零；這正確區分首位與一般數位。所有模加法的兩個加數都小於 MOD，相加不超過有號三十二位整數上限。dp.swap(next) 換到下一層，最後各筆查詢直接輸出已算好的表格值。
