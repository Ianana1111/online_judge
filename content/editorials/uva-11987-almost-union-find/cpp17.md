parent 的容量一次配置 n+m+1，used 從 n 開始，每個真正搬移只增加一次。id 僅對一至 n 的實際元素定義，不為歷史節點新增元素；query 永遠先 root(id[p])，因此能找到最新所在集合。

size 與 sum 只需在根上有效，搬移新葉的陣列欄位保持零不影響查詢，因為查詢會走到 b。weight 與 size 用途不同：weight 保留歷史節點並在新葉加入時加一，確保合併的樹高界；size 僅統計有效元素。舊根即使曾代表 p，也不需刪除或拆子樹。sum 用 long long，搬移加減的是原元素值 p，不是新的虛擬節點編號。
