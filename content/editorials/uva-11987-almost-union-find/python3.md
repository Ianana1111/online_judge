關鍵是分開「數字 p」與「並查集節點」。如果直接把 p 的舊節點掛到另一個根，原本指向它的其他元素也可能被帶走。因此用 id[p] 保存目前代表 p 的節點；移動時扣掉舊集合的數量與總和，再建立一個全新的節點，讓它指向目標集合。

查詢與合併都先找 id[p] 的根。根上的 size 是真正元素數，weight 是並查集樹的合併權重，兩者不能混在一起：移動會留下空的舊節點，但不代表樹的結構縮小。每次移動最多增加一個節點，所以 n+m+1 的陣列足夠；集合總和使用 64 位元。
