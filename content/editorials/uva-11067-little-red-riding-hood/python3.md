讀取迴圈先取得 width，再讀 height，終止符號不再當作正式案例。blocked 是座標集合，ways[0]=1 表示原點的起始空路徑；第一列在沒有障礙時會自然向右傳遞，第一欄則直接保留從下方到達的計數。

每格先檢查阻擋，只有未被阻擋且 x>0 時才加入左邊計數，因此不會讀到負索引。Python 整數保留所有中間精確值，不需錯誤地以最終上限取模。最後分三個分支組合完整英文句子，單引號採一般 ASCII 字元，並將不同案例各自換行輸出。
