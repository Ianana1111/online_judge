There are two kinds of connectivity: a die includes both `*` and `X`, while a pip includes only `X`. First run four-direction BFS from each unseen non-background cell and collect the entire die. Then inspect its cells; each unseen `X` starts another BFS restricted to `X` and contributes one pip.

Keep separate visited arrays and leave the picture unchanged. The C queue stores coordinates as `row * width + col` within the fixed image bound. Sort the resulting pip counts before printing. Diagonal contact never joins components.
