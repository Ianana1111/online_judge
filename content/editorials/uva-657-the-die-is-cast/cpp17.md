dieSeen 與 pipSeen 初始相同大小但各自保存不同資訊。第一層入隊時立即標記，避免同一格被鄰居重複放進 queue；cells 保存此骰子的座標，讓後續只掃該骰子。

第二層遇到未見 X 才先加 dots，再將整片 X 標記。兩個 BFS 共用四個方向向量與 inside 邊界函式，確保連通定義一致且不越界。answer 是 vector，保留多顆同點骰子；排序後用位置控制空白分隔，每個 Throw 之後補一個空行。
