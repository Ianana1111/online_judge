質數表大小設為 2001，讓最大合法頻率 2000 可以直接作索引。p² 超過上界後不必繼續篩，因為任何尚未處理的合數必定已有較小質因數。

每輪案例重新宣告 `array<int,128> count{}`，大括號會把所有位置歸零。以 unsigned char 讀取字元值，不進行大小寫轉換；題目的英數字都在 ASCII 陣列範圍內。最後依索引遞增讀取 prime[count[ch]]，所以零次數不會誤入答案，符合者也已排好序。answer.empty() 統一處理沒有任何符合字元的情況。
