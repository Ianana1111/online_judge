路徑雖然沿對角線繞行，但每個點都能分配一個唯一序號。座標 `(x,y)` 所在的對角線是 `d=x+y`；前面完整對角線共有 `1+2+…+d=d(d+1)/2` 個點，而同一條線從 `x=0` 起走，所以再加上 x。這就是 `position(x,y)`。起點到終點走過的邊數等於終點序號減起點序號，不必模擬長路徑。座標可達十萬，三角數會超過 32 位元，C 與 Java 在乘法之前就使用 64 位元型別。

python3 程式把這個推理直接轉成計算或查表，並依題目規定讀取每一組資料與輸出答案。
