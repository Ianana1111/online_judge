streak 與 total 都宣告在每組內並初始化零，確保兩份考卷不會連在一起計算。O 分支的 ++streak 位於 total+=streak 之前，讓第一個 O 得一分。

X 分支只修改 streak，不影響過去已累積的分數。輸入保證只含 O、X，所以 else 就代表中斷。最後只輸出 total，不需要保存每題分數或每段長度。對單一 O、單一 X、全對、全錯和交錯結果，都是同一套狀態轉移。
