`mask` 的第 0～6 位依序對應上、右上、右下、下、左下、左上、中。`segments & (1 << bit)` 非零，就表示目前數字需要那條線段。十六進位常數只用來簡潔保存七個布林值，與輸入數字的進位無關。

橫線分支依 row 選出上、中、下的 bit，再建立長度恰為 size 的線段或空白字串。直線分支則根據是否在上半部，選擇左右線段的 bit，中間始終印 size 個空白。

最內層 index 大於 0 才印位數間隔；每完成一整列換行，每完成一整組再換行。number 使用字串保存，方便由左到右查每一位數，包含單獨輸入 0 的顯示情況。
