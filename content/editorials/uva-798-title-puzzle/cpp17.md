count_tiles 建立每組數量的混合進位倍率；第 i 組剩餘量可用除 multiplier[i] 再取模初始數量加一還原。每個可能位置的格子遮罩預先保存於 anchors，按其左上角格子分組，搜尋時不用重新掃描矩形。

solve 中 full^occupied 的最低一位就是第一個空格，ctz 找出其索引。mask&occupied 不為零表示重疊，不能放；合法分支用位元或增加占用格，並扣對應混合進位倍率。填滿時同時檢查 code 為零，確保磁磚全數用完。area≤20，加上至多二十片帶來的混合進位資訊，狀態鍵低於四十一位，使用 uint64_t 不會截斷。每個新拼圖都清空 memo。
