position 以 −1 初始化，代表王子沒走過該格；真正第一格的位置零必須與不存在分開。兩個讀取迴圈都包含 p 或 q 的最後索引，所以完整讀 p+1、q+1 個格子。

tails 保持遞增，lower_bound 找第一個至少為目前位置的結尾。沒有這種結尾時，新值能延長現有最長序列；否則用較小位置取代，使同長度路線更容易接續。只有不在王子路線的值會被跳過，輸入仍全部讀取。輸出 tails.size，不轉換成跳躍次數，也不受棋盤畫法或移動距離影響。
