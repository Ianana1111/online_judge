`digitValue` 分別處理數字、大寫與小寫區間，各自加上正確的起始值。主迴圈略過可能存在的正負號，再同步累加 sum 及更新 maximum，不需要逐進位重新解析字串。

base 從兩個合法下界中的較大者起算，避免 base−1 為零。檢查使用整數取餘數，找到第一個零餘數就 break，因此 answer 自然是最小解。answer 保持 −1 代表所有進位都失敗，此時印出指定的完整句子。零與帶號零都保留正常處理，EOF 才停止。
