parse 每次先讀可帶符號的整數，再讀下一個二元運算子；match.end 推進的是這次匹配含空白的完整長度。render 將同一組 values、operators 全部印回，保留原變數的大小寫，整數轉文字會自然正規化前導零與正號。

high 列出尚未處理的乘除索引，high[0] 即目前最左者；没有高優先級才選索引零。計算後切片替換兩數、pop 一個運算子，維持交錯對應。每輪都立刻印一次，初始狀態也在迴圈前印出。程式使用 Python 精確整數，並解除執行環境對整數十進位轉字串的預設位數限制。
