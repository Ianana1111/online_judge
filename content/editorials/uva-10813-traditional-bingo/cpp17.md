card 全部初始化為零，讀入時跳過 [2][2]，所以免費格自然保留零。called 也初始化為零，對全部 75 個叫號保存從 1 開始的時間，中心的 called[0] 不會被覆寫。

外圈 r 同時處理第 r 橫列和第 r 直欄；內圈以 max 累積該線最晚叫號。每完成一條列或欄便更新 answer 的最小值。兩個 diagonal 變數跨外圈累積兩條對角線的最晚時間，最後再納入比較。讀完全部輸入才求答案，可完全避開提早停止讀取的問題。
