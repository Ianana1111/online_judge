The first four coordinates describe one lower-left and upper-right pair, and the next four describe the other. Width and height use continuous coordinate differences with no endpoint adjustment.

Each overlap dimension is clamped independently. `strong` is intersection, `weak` explicitly subtracts it twice, and `none` subtracts the two disjoint guarded categories from 10,000. Output preserves the required Night numbering and category order.
