網路頻寬要同時考慮所有可走的路，不是找容量最大的一條路。先把每條無向連線的容量加到兩個方向，同一對頂點的平行連線也要累加。殘餘容量記錄還能增加多少流；推送 f 單位後，正向扣 f、反向加 f，這個反向容量讓後續路徑能修正早先的選擇。

Dinic 先用 BFS 把可達頂點分層，再只沿著下一層的邊送流。DFS 的 next 指標記住已經檢查到哪一個鄰居，避免重複掃描不能再用的邊。當這一層的流送不動，就重建分層；連 BFS 都到不了終點時，才表示沒有增廣路徑，當前流量就是答案。這題最多 100 個頂點，使用容量矩陣能讓平行邊處理與殘餘更新很直觀。
