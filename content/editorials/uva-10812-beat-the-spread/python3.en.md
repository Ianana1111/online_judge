The input is parsed into Python integers, whose precision does not impose an unstated bound. After the test count, each fixed two-value slice supplies one complete case.

The conditional rejects both failure modes: total below difference and odd `total-difference`. Integer division is executed only after divisibility has been proved, so it performs no accidental rounding.

`higher=total-lower` recovers the larger value directly. Passing both integers to `print` inserts the required single separating space.
