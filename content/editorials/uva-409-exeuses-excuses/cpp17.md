keywords 是集合，查詢的單位是完整的 token。finish 每次最多加一分，再清空 token；連續標點會產生空 token，但空字串不在合法關鍵字中，所以不影響分數。

getline 讀取原句並保留開頭與結尾空格，只移除 CRLF 的 CR。score 每組重新歸零，best 初始為零，因此全部零分時所有行都會輸出。最後按輸入順序印同分者只是其中一種合法順序，checker 會核對完整多重集合，不要求一定依這個順序。
