{
  "sources": [
    "https://onlinejudge.org/external/4/409.pdf"
  ],
  "constraints": [
    "K,E1..20;keywords lowercase length1..20;excuses<=70ASCIIlettersdigits spacesquote.,!?withnonspace;alphabeticwordboundaries;caseinsensitive;eachoccurrencecounts;exactoriginalexcusesANYtieorder,multiplicityretained."
  ],
  "independentMethod": "Independent per-keyword regex boundaries excluding ASCII letters, summing every match while preserving original lines; compare maximum-score excuse multisets with exact data whitespace.",
  "oracleFile": "scripts/editorials/oracles/lexical_witnesses.py",
  "mutations": [
    {
      "id": "drop-final-word",
      "label": "Does not score a final alphabetic token",
      "languageKey": "cpp17",
      "find": "finish();best=max(best,score[i]);",
      "replace": "best=max(best,score[i]);"
    },
    {
      "id": "digits-not-boundaries",
      "label": "Merges letters across digits",
      "languageKey": "cpp17",
      "find": "else finish();",
      "replace": "else if(ch<'0'||ch>'9')finish();"
    },
    {
      "id": "legal-alternate-output",
      "label": "Another permitted ordering, spacing or shortest path",
      "languageKey": "cpp17",
      "find": "for(int i=0;i<e;++i)if(score[i]==best)",
      "replace": "for(int i=e-1;i>=0;--i)if(score[i]==best)",
      "expectation": "AC"
    }
  ],
  "checkerChange": {
    "from": "IGNORE_TRAILING_WS",
    "to": "SPECIAL",
    "reason": "Respect the original semantic output contract: exact original excuse/name multisets may be reordered, complete anagram lists retain all multiplicities withconsecutive numbering, and algebra solutions retain all required operator/number states while allowing explicitly insignificant spacing. Validate all content and reject missing/extra records."
  }
}
