previous 保存本條路線上一站，讀到零才結束該線；每次相鄰站都加兩方向，不把整條路線誤建成完全圖。seen 每線重建，lines[v] 只對不同路線各加一次。

important 依站號自然遞增。每次 BFS 使用全新的 distance，以 −1 表示尚未到達；次要站同樣會入列，因為最短旅程可能經過它。加總階段才限制為重要站，best 只在 sum<best 時更新。輸出加回一基站號，並使用題目指定的英文前綴。
