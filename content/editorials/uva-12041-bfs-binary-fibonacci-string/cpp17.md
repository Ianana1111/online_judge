length 表只保存零至四十七階，long long 能容納所有表值。at 迴圈每次選擇一個真正包含目標位置的子字串；若在右段，先減掉左段長度再把階數減一，左段則階數減二而位置不變。

讀取的 n 用 long long，n>47 才採用奇偶公式，較小階數仍按原定義處理。左右索引也用寬整數，position++ 在最大允許索引後不會溢位。reserve 只配置實際要輸出的至多一萬零一個字元，最後每個查詢輸出一整行，沒有多餘前綴或空格。
