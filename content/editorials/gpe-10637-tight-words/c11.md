先按最後一個數字分類。長度一的字串，每個數字各有一種；增加一個數字 d 時，前一個數字只能是 d−1、d、d+1。用兩組陣列輪流計算，就能數出所有符合相鄰差不超過一的字串。

分母是 (K+1)^N，因為每個位置都可以選 0 到 K，前導零也算。字串數雖然很大，但最後百分比只有 0 到 100；不用浮點數，先精確求 `floor((2·100·有效數·10^5+總數)/(2·總數))`，就得到四捨五入後的五位小數整數表示。C／C++ 用大整數比較配合二分搜尋 0..10^7 的商；Java 用 BigInteger 除法。整數部分與剩下五位分開印，尾端零必須補齊。
