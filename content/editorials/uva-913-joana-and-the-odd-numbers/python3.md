輸入的 N 是該列「有幾個數」，不是列號。因為第 r 列有 `2r−1` 個數，先反推 `r=(N+1)/2`。前 r 列總共用了 `1+3+…+(2r−1)=r²` 個正奇數，而第 k 個正奇數是 `2k−1`，所以本列最後一數是 `2r²−1`。最後三數依次相差二，總和為 `3 × last−6`。不必真的建表或列出任何數字；輸入讀到 EOF，C 和 Java 的乘法全程使用 64 位元整數，避免中途溢位。

這份 python3 程式將上述推導直接寫成迴圈或公式，並保留題目要求的輸入結束與輸出格式。
