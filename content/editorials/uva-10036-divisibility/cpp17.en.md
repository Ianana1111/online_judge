The first number initializes the state by itself and processing begins at index one, matching where signs may be inserted. Double modulo normalizes a possibly negative first value and every later value.

Each iteration allocates a cleared `next`. Both transitions read only the old `possible`, ensuring the current input is used exactly once. Adding `k` before the subtraction modulo keeps the index nonnegative. After moving the completed state forward, only remainder zero controls the required output phrase.
