half_sums 先計算每個遮罩的和：移除最低的一位後使用已算好的子集合和，再加該元素，總共 O(2^h) 次計算。排序只改變列舉順序，遮罩本身仍保留元素身分。

lower_bound 與 upper_bound 的第二欄分別使用零與 UINT32_MAX，確保包含相同和的所有遮罩。答案排序先看個數，再用 difference&-difference 取得最低不同位元；兩遮罩完全相同時回傳 false，維持合法的嚴格排序。每組前的 first 控制空白行，無解也照樣分隔，不輸出零個子集合的標頭。
