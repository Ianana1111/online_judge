`degree` 由係數個數減一取得，`coefficients[:-1]` 排除最後的常數項。列舉係數時，`degree-index` 正是該項原本的次數，因此微分後的係數為兩者乘積。

`value` 每輪先乘 x，再加下一個導數係數，實現 Horner 的由高次往低次累積。Python 的整數不會像固定寬度有號整數那樣溢位，負 x、負係數與常數多項式也使用同一套流程。

主程式用同一個行迭代器讀資料：for 取得 x 所在行，`next(lines)` 取得緊接的係數行。每次只輸出一組答案，不需要額外測試筆數，也不把空白當成多項式中的零係數。
