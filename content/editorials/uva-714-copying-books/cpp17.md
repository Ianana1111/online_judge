二分的 groups 由一開始，只有 current+x>limit 才開新組，等於上限仍可放入。lo 已不小於每本頁數，所以不存在單本超限但仍被錯放的情形。

重建的 groups 表示目前尚需安排多少組，i+1 是包含當前書在內的剩餘本數。若 i+1<groups，必須先結束已累積的右組，把當前書留給前面的人。split[i] 表示在第 i 本後印斜線；掃描輸出時數字前统一補一個空格、斜線前也補一個空格，下一個數字再補空格，得到要求的格式。
