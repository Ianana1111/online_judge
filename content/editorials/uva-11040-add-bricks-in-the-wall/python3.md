wall 的各列長度依 r+1 建立，與三角形结构相同。輸入恰有十五個已知數，依偶數 r、c 寫入，position 持續向後而不依賴實際換行排版。

恢復時 left、right 是下兩層已知的兩端，middle 填在它們之間，再填上方相鄰兩格。Python 的 // 在這裡不是近似取整，因為合法資料保證可整除；負數也得到精確解。輸出逐列 join，以單一空格分隔，組間不額外加入題目未要求的空白行。
