先檢查點是否落在兩條分界線上：只要 x 等於中心的 x，或 y 等於中心的 y，就輸出 `divisa`，不能硬塞進任何象限。排除邊界後，比較 y 決定北 N 或南 S，再比較 x 決定東 E 或西 O；西方的字母是 O，不是英文的 W。每組先讀查詢數及中心座標，接著恰好讀指定數量的點。查詢數為零才停止，座標本身出現零並不表示結束。

C 版本以 `scanf` 讀取完整資料，使用題目範圍內足夠的整數型別；每筆答案用 `printf` 單獨輸出。
