unique 同時移除不同點對生成的重複直線遮罩。叉積寫成 x*dy==y*dx，不需要除法，所以水平、垂直與負斜率都以同一規則處理。兩個相同位置不定義唯一方向，先由 same 遮罩處理，再略過這種點對。

dp 的初始大值 n+1 代表尚不可達，至多 n 次單點射擊一定足夠。mask|line 不變表示浪費一槍，直接略過；其他轉移必往較大的數字移動。已達 m 棵的狀態只更新 answer，因為繼續射擊不可能降低次數。位元數以 popcount 計算，輸出編號從一開始。
