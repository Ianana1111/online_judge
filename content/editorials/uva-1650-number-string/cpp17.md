previous 長度就是目前排列元素數 k，索引是零起算的最後名次。prefix 長度多一，prefix[j] 嚴格表示前 j 個名次的總數，因此 I 分支直接使用 prefix[j]，D 則用總數減掉它。

插入新元素後有 k+1 種末名次，所以 current 的 j 從零跑到 k，兩端也要處理：I 時 j=0 沒有合法前驅，D 時 j=k 同樣沒有。問號在每種 j 都可接上所有前驅。current 算完才 swap，不會把新一輪資料混入舊前綴；最後再把不同末名次的結果累加成一個模數答案。
