prime 是質數篩表，從每個質數平方開始劃掉倍數，較小倍數已由先前質數處理。powers 的每個數都以 1LL 確保平方運算先提升到 64 位元，再沿同一質數逐次乘下去。

limit 明確設成 10^12−1，符合題目的嚴格上界。value>limit/p 表示下一次乘法一定超過上界，直接 break，同時避免溢位。排序只做一次，後續各區間直接使用兩個標準二分函式，不必重新分解每個區間數字。upper_bound 與 lower_bound 的差是元素數，不是兩個數值的差。
