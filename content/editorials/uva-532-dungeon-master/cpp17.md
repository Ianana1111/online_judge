grid 保存 levels*rows 個字串，每個字串長 cols；z*rows+y 是地圖列索引。起點與出口則用完整的一維 id 保存。還原 id 時先取欄餘數，再除 cols 取列餘數，最後除一層格數取得樓層。

distance 同時是最短距離與已訪問記號，只有 −1 可以入隊。邊界檢查在任何陣列存取之前；通過後才組合 next。六個位移陣列的同一索引代表一個完整方向。最後輸出固定句子與句點，不把起點也算作一分鐘。
