每組建立全零 seen，而 epoch 從一開始，保證最初沒有任何值被誤認為已出現。先比較 seen[value] 與 epoch，若相同才增加答案與版本；第一次讀到某值不能直接構成區間。

最後一行 seen[value]=epoch 在版本更新之後執行，這個順序是關鍵：同一位置既能是剛選區間的右端點，也會被保留成新區間的左端點。當沒有選區間時，這行只是一般的加入已見集合。程式不需存整段輸入，也不需實際找出重複值的舊索引，因為答案只要求數量。
