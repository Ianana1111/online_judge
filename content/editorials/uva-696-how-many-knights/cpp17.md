rows、columns 保存原輸入；shorter、longer 只是另外建立的分類變數，最後輸出仍用原始方向。短邊零時直接沒有格子，一時全部可放，二時用四欄週期處理。

一般棋盤以 (rows*columns+1)/2 做整數向上取半；雙行尾端使用 min(4,2*(longer%4))，餘一欄加二、餘二或三欄都只加四。迴圈只在兩個維度都零時停止，單個零維度仍可正常得到零隻。輸出完整英文句子與句點，包含單數數值時也沿用題目固定的 row、column 字樣。
