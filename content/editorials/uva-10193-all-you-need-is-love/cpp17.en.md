`value` performs exact Horner parsing in base two without floating powers. The parsed integers are passed to standard `gcd`, and the condition is strictly greater than one.

The boolean selects between the two fixed sentences while case numbering begins at one. Output preserves `Pair #`, the colon and following space, and the final exclamation mark. Inputs are guaranteed valid positive binary strings and are never interpreted as sentinels.
