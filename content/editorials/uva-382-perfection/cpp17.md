標頭在讀取迴圈前印一次，結尾在零或 EOF 結束後印一次。初始化 total 時先處理 n=1；對其他正整數，1 是真因數，因此從一開始。

枚舉從二起步，避免再加入 1 或 n。找到可整除的 divisor 後，先加入較小因數，再於 paired 不等於 divisor 時加入較大因數。分類運算只做大小比較，不依賴任何預存完全數清單。`setw(5)` 對齊當前數字，後面的字串含有明確兩個空格，維持正確輸出契約。
