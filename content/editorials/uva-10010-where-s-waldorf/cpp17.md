`lowercase` 將字元先轉成 unsigned char 再交給 tolower，避免以負的 char 值呼叫字元函式。題目雖然只含英文字母，這仍是安全的 C++ 用法。

四層搜尋依序為列、欄、列方向、欄方向，`found` 同時出現在各層條件中，找到完整單字後就停止。字元迴圈先判斷範圍，再存取 grid，利用短路求值避免越界。

輸入的空白分隔行由 `cin >>` 自動略過，因為網格與單字本身不含空白。每組第一個答案前，只有不是第一組時才補空白行；每筆座標則以 r+1、c+1 轉成從 1 開始。
