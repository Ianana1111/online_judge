factorial 保存各階乘模值，inverse 從 1000! 的費馬反元素往下回推，因為 (n−1)! 的反元素等於 n 乘 n! 的反元素。choose 的兩次乘法中間皆取模，避免三個模值直接相乘。

valid 專門計算選定固定集合後的合法排列，j 最多 M−K，保證 n−k−j 非負。奇數項先加 MOD 再減，維持合法非負模值。最後乘 choose(m,k) 才把所有固定集合加總；輸出格式為 Case 編號與答案，前面沒有井字號。
