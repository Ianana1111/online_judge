Frequencies and prefix counts make threshold populations constant-time. Enumerating only appetites actually present still covers every breakpoint. `3*p < largest*q` rejects candidates requiring a fourth visit using integer arithmetic.

`visits` begins with three per student, then subtracts once for everyone needing at most two and again for everyone needing one. Numerator and denominator remain exact, candidates compare by cross products, and only the final winner is reduced with gcd. Denominator one is printed as an integer.
