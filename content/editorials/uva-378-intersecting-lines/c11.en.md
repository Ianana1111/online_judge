Write the lines as `A+t*u` and `C+s*v`, with `u=B-A` and `v=D-C`. If `cross(u,v)` is zero, check `cross(C-A,u)` to distinguish coincident from distinct parallel lines. Otherwise `t=cross(C-A,v)/cross(u,v)`, giving integer numerators and a common denominator for both intersection coordinates.

The ±1000 coordinate bound keeps intermediate products within `long long`. Round the final rational coordinates to two decimals using integers; do not use floating point to classify parallel lines, and avoid printing a rounded negative zero.
