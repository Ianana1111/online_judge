把兩條直線寫成 `A+t·u` 與 `C+s·v`，其中 `u=B−A`、`v=D−C`。先算 `cross(u,v)`：零表示平行，再看 `cross(C−A,u)` 以區分重合與沒有交點。若不為零，參數 `t=cross(C−A,v)/cross(u,v)`，可直接得到交點兩座標的整數分子與共同分母。

座標界限只有 ±1000，`long long` 足以保存中間乘積。最後才把有理數乘一百並以整數作兩位小數捨入，避免先用浮點數判定平行，也不把四捨五入後的負零印成 `-0.00`。
