For each cell and direction, the while loop skips equal characters and records only an in-bounds first different cell. Initial cost is zero only at top-left.

Each target copies current costs into Dijkstra distances and pushes all finite sources with their actual values. Stale heap entries are ignored. Filtering keeps only matching cells. Enter is appended before compression, while `selections` is computed beforehand, preserving every physical select press.
