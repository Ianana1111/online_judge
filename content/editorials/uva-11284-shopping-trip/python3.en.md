`cents` splits the decimal token and constructs an exact integer amount without `float`. The distance matrix uses `None` for an unknown connection, zero on its diagonal, and the cheapest value among parallel roads. Floyd-Warshall combines only existing path segments.

The `savings` dictionary adds offers sharing a store. `stores` places home first, and each DP transition sets one new bit, guaranteeing termination. The immediate-return value includes the final trip home. Output uses integer division and two-digit padding, while nonpositive results use the exact required stay-home message.
