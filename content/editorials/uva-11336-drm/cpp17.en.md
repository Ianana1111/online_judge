`readMap` sees the first asterisk and consumes the other two, preventing terminator tokens from leaking into the next map. `consistent` collects both endpoint sets and verifies retained old places before creating components.

Only added places receive DSU IDs. Attachments are recorded after all unions so every stored ID is a final root. Direct old-old edges are normalized by lexicographic endpoint order. Two ordered attachment sets are intersected with advancing iterators, and the final Boolean controls both `YES/NO` and the word `not` in the required sentence.
