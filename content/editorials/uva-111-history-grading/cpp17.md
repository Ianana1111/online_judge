getline 將一整行交給 istringstream，空白行直接略過。只有一個數字代表新的題目規模，此時清除 correct；下一行的完整排列會被保存為老師名次，而不當成學生評分。

核心指定是 sequence[ranking[event]-1]=correct[event]，左側用學生名次決定位置，右側用老師名次決定數值。dp 預設一，允許只保留目前事件；內層遍歷所有先前位置，不限制相鄰。每個學生都建立新的 dp，避免前一份答案的狀態影響目前分數。
