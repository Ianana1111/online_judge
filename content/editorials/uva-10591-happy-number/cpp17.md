`nextValue` 用 `% 10` 取出個位數、`/ 10` 移除個位數，將 `digit * digit` 累加到 `total`。N 小於十億，轉換結果更小，使用 `int` 足夠。

主程式把原始輸入放在 `original`，另一個變數 `value` 專門負責模擬。`seen` 建立在每筆測試的迴圈內，所以不同測試不會互相影響。

`while` 的兩個條件分別排除成功抵達 1，以及回到已拜訪狀態。先插入目前值，再做轉換，確保下一輪能辨識循環。最後只需檢查 `value == 1` 就能選擇完整輸出句子，並以從 1 開始的 `caseNo` 編號。
