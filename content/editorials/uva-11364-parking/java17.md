所有商店都在最左位置 L 與最右位置 R 之間，拜訪全部商店一定會走過這整段。車停在 L，走到 R 再回到 L，就能以 `2 × (R−L)` 達到最短距離；中間有多少商店、輸入順序如何，都不會改變答案。程式對每組資料只找最小與最大位置，不必排序。若只有一間商店或所有位置相同，兩端差為零，答案自然也是零。先讀組數，再依每組的商店數讀完所有位置。

Java 版本以 `Scanner` 逐項讀入，使用足夠的整數型別，並依題目的結束條件停止。
