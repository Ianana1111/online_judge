程式以兩個 long long 保存速度與時間，使用 `while (cin >> velocity >> time)` 反覆讀取直到 EOF，沒有額外的數值終止條件，因此 `0 0` 正確輸出零。

輸出運算式直接實作推導出的兩倍中點速度乘時間。乘法保留速度的正負號，t=0 時自然得到零；不使用 abs，也不假設初速度或加速度的額外值。每次迴圈只輸出一個整數和換行，沒有案例編號或小數格式設定，符合題目要求。
