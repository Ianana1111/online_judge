先從物理關係找答案，不必猜初速度或加速度。等加速度代表速度隨時間等量變化；從 0 到 `2t` 的平均速度，就是時間 `t` 的速度 `v`。因此整段位移為 `v × 2t`。

`Scanner.hasNextLong()` 讓程式一路讀到 EOF，包含 `0 0` 這組資料。每次讀兩個 `long` 並計算 `2 * velocity * time`。不使用浮點數，也不要因為速度是負數而改成正值。
