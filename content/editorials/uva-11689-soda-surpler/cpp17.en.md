`empty` combines both initial sources, while `total` starts at zero. Integer division computes all exchanges available in a round at once.

The update retains both the modulo remainder and the empties returned by `drinks`. The loop condition includes exact multiples. Since `cost>1`, each successful round reduces the stock and termination is guaranteed without any borrowing step.
