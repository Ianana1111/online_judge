`seen{original}` records the start immediately, while `current` changes independently of the heading. `to_string(0)` yields one zero character, so an internal zero continues correctly.

The subtraction is printed before membership testing. Only a new result is inserted and assigned to current, while length increments for every printed transition. Each case ends with the specified chain line and blank line, including the final case.
