`printNumber` 的第一個分支先遞迴印出 kuti 的數量，之後才輸出單位，順序不能反過來。每個單位處理完都以取餘數移除已輸出的部分，使後續分支只看到尚未表示的數值。

所有文字片段都自行帶一個前導空格，所以主程式只需先印案例編號與句點，不必維護 first 標記。`setw(4)` 只作用在緊接的案例編號，使用預設靠右空格填補；數字零由主程式特判，printNumber 內則略過零，兼顧真正的零輸入與遞迴餘數。
