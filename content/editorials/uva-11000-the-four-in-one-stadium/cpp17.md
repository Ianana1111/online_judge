每筆輸入都從 male=0、female=1 重新開始，year 迴圈恰好執行 n 次。nextMale 和 nextFemale 先各自讀舊 male，兩者計算完成後才寫回，這是避免更新順序錯誤的關鍵。

使用較寬的無號型別是因為題目允許接近 2³² 的非負計數，而非讓任意大年份都變成合法輸入。while 條件只排除負的終止記號，零年會進入輸出但不執行繁殖迴圈。輸出兩欄以空格分隔，沒有案例標號。
