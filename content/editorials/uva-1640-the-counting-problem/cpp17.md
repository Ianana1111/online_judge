prefix 初始化十個計數為零；n=0 時 factor<=n 不成立，自然返回空範圍結果。higher、digit、lower 分別用整數除法、取十餘數、取位權餘數取得，不需把每個範圍數轉字串。

非零數字放在同一個迴圈，零的特殊分支只有 higher>0 才進入，避免 higher−1 為負及最高位前導零。主程式先判哨兵再交換端點，呼叫 prefix(a−1) 與 prefix(b) 並逐欄相減。空白只印在非第一欄之前，最後換行，符合十個數字以單一空白分隔的格式。
