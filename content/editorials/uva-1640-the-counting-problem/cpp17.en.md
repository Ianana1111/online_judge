`prefix` begins with zeroed counters, and n=0 skips the factor loop naturally. Integer division and remainder obtain higher, current, and lower portions without converting ranges to strings.

Digits one through nine share one loop. Zero enters its formula only when `higher>0`, preventing both a negative `(higher-1)` term and leading positions. The main loop rejects the sentinel, swaps reversed endpoints, subtracts prefix arrays, and prints exactly ten single-space-separated fields.
