程式先分開處理冠軍，因為零沒有最低一位元，也沒有輸球對手。__builtin_popcountll 在非零 X 上直接取得祖先數，加一才是排名而非比自己強的隊數。

block 的 x 與二補數相與只留下最低的一位元。變數是 unsigned long long，所以取反加一的模運算有明確定義；1ULL 確保左移在寬型別進行。最後由全部隊伍數扣掉 block−1 個必然落後者得到最壞位置。每組獨立輸出兩個整數，不需要保存任何賽程。
