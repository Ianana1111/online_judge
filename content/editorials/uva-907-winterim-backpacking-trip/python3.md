外層按照 n+1 讀距離，at 隨每組的實際長度前進。low 設為最長單段，保證判定時任何單段都可放進一天；high 是全部和，保證存在可行上界。

walked 是目前這天已走的距離。只有嚴格超過 limit 才換天，並把新段當成下一天的起始；剛好等於 limit 不需要提早換。days 一開始是一，不是零，因為即使沒有住宿也有一天步行。比較時使用 nights+1，允許後續補住宿天數。二分完成後 low 等於 high，就是能做到的最小最長距離。
