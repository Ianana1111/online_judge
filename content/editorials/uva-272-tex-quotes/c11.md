引號可能跨越好幾行，所以不能逐行找配對，也不能每到換行就重設。把整份輸入看成連續字元流，只記錄「下一個雙引號是開頭嗎」這一個狀態。初始為開頭；每遇到雙引號，依狀態寫出兩個反引號或兩個單引號，再翻轉狀態。所有其他字元，包括空格、換行和原本的單引號，都必須照原樣輸出。這樣第奇數個雙引號一定是開頭，第偶數個一定是結尾。

`getchar` 能讀到空白與換行，`fputs` 只替換引號，`putchar` 保留其他字元。
