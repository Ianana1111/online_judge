外圈以較寬的 n 讀取完整的 32 位無號範圍，零只結束讀取。low 與 high 都是 unsigned long long，mid 的平方也在同一個安全型別計算。

迴圈條件包含相等，因此最後只剩一個候選根時仍會檢查。mid 最小是一，只有 value>n 才會執行 high=mid−1；合法 n≥1，所以不會在 mid=0 時發生無號下溢。找到平方會直接設 square 並停止；若區間耗盡仍沒找到，輸出 no。每個案例都重新初始化搜尋範圍與結果，沒有保留上一盞燈的狀態。
