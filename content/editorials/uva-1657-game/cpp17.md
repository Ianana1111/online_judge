solve 依 X 遞增、Y 遞增產生每個無序對一次。alive 是目前尚未排除者；value 依 turn 奇偶選和或積，count 僅統計當下 alive 的資料。先完成全部計數，再收集 remove，最後統一改 alive，因此同輪資訊不會混用。

answers[turn] 收集的正是該輪能確認者，而不是下一輪的 survivors。每個數對只會被刪一次，快取全部輪次不會複製一百份全集。連續兩次 remove 為空才提早結束，單一玩家本輪沒有新資訊時，另一位仍可能知道。主程式 map 按 N 共用預算結果；輸入到 EOF，每筆先印答案數，再印數對，不插入未要求的空行。
