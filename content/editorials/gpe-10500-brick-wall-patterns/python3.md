從牆的最左邊分類：若第一塊磚直放，就填滿一整欄，剩下寬度 N−1；若第一塊橫放，下方也必須橫放一塊，兩塊一起占兩欄，剩下寬度 N−2。這兩種情況互斥且涵蓋全部鋪法，所以 `ways[N] = ways[N−1] + ways[N−2]`。空牆算一種完成方式，設 `ways[0]=1`，寬一也只有一種。先算到五十再回答查詢；零是結束標記，不輸出。第五十項超過 32 位元，C 和 Java 要用 64 位元整數。

這份 python3 程式將上述推導直接寫成迴圈或公式，並保留題目要求的輸入結束與輸出格式。
