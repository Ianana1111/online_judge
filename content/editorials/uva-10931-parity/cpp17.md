外層讀取條件先判斷輸入是否成功，再排除終止符 0。`bits` 與 `ones` 都在每筆輸入內重新建立。

內層每次取餘數得到 0 或 1，轉成字元後附加到 `bits`，同時把該位元加到 `ones`。接著整數除以 2，直到剩餘值為 0。此處消耗掉原始數字沒有關係，輸出格式只需要二進位字串與計數。

`reverse` 將低位優先的順序倒過來。最後直接輸出 ones，不先做模二運算，因為 `(mod 2)` 是題目要求印出的文字，範例中的 P 仍是未取餘數的 1 的個數。
