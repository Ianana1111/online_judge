coins 保存最少張數，初始只有面額一，因此從零到 H 的最少張數正好等於郵資。search 參數包括已選面額數、最後面額、目前連續範圍與 DP 表，這些資料足以决定所有後續選擇。

每個候選新面額比 last 大、至多 coverage+1。next 長度是 H 倍新面額加一，足夠包含所有最多 H 張的新郵資；因新面額較大，它也一定容納完整舊表。更新時從 denomination 向上掃，可反覆引用同一面額。range 從已確定可達的 coverage 繼續往右，只在張數至多 H 時延長。搜尋深度到 K 後更新 best，其他分支用反覆套用的安全上界決定是否剪枝。

cache 以 (H,K) 當鍵，不依前一組輸入的全域狀態沿用 DP。每次新查詢都重設 best 與初始表，重複查詢直接讀回已完成的結果。
