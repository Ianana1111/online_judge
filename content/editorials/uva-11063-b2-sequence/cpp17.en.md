The full vector is read before checks. Positivity is tested for every element, and `<=` against the predecessor rejects both decreases and equality.

A fresh `sums` set belongs to each case. The Boolean returned by `insert` identifies duplicate pair sums. `long long` performs addition safely without using values as array indices.

The case counter and fixed sentence include an optional `not`, followed by two newlines to preserve the specified case spacing.
