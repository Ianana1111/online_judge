a 先完整讀入，因此後續即使判失敗也不會使下一組輸入錯位。相鄰檢查使用 <=，明確排除重複值；對第一個元素則只檢查是否正數。

每組建立新的 sums 集合。insert 回傳的 second 表示是否真的新增，若為 false 就把 good 設為 false，但仍可繼續檢查其他配對。long long 讓加法在較寬範圍中運算，不使用和值當陣列索引。輸出以同一個固定句型搭配可選的 not，再加兩個換行，保留題目要求的案例空行。
