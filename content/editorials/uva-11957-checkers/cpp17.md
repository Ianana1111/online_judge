讀取時找到 W 就把對應 ways 設一；棋盤保持原樣，因為白棋只往上，後續落點不可能回到它原來的位置。nextRow、nextCol 先設定成一步斜上，再依是否遇到 B 決定加走一步，兩種情況共用最後的落點檢查。

外層 row 由 n−1 遞減到一，頂列不會被拿來继续移動；跳躍雖跨過一列，仍指向尚未處理的較小列，因此不影響順序正確性。每次累加都取指定的 MOD，最後同樣取模加總頂列。輸出 Case 編號冒號與空格，與範例一致。
