key 的 26 個位置直接對應 A 至 Z。範圍迴圈以 char& 取得原字元的參考，在判定它是字母且累加計數後，直接替換成表中的數字。

連字號的分支只增加統計，不修改文字；0 與 1 不符合任何分支，因此自然保留。每行重新初始化兩個計數，最後依本站格式輸出轉碼字串、字母數、連字號數，中間各一個空格。while(cin>>text) 讓所有合法字串，包括單一 0 或 1，都能被處理到 EOF。
