ways[0] 的一不是一枚硬幣，而是空組合，讓每個面額都能從零開始建立合法方法。內層 sum 遞增，所以 ways[sum−coin] 已可包含同一面額，符合無限使用。

讀入 token 後分開整數與小數部分，在小數後補零便能處理一位或兩位小數表示。合法金額超過兩位的小數只能是零，因此取前兩位不會損失分數。重新用整數分數組出兩位小數的 amount，再以兩次 setw 分別對齊欄位；setw 只作用於下一個輸出項目。
