輸入矩陣以 char 逐格讀取，再減去字元零；cin 會略過空白，因此能讀連在一起的數字列，也可讀分隔的數字。row 直接 swap 兩個 vector，col 才逐列交換格子。

transpose 的內層從 r+1 開始，確保每對只交換一次。inc 與 dec 共用 delta 分支，九在模十意義下等於負一，所有儲存值始終在零到九。最後依矩陣現在的排列逐格輸出，不讀回原始資料，並依格式保留 Case # 和空白行。
