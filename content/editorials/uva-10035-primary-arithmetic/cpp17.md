外層條件以 `a != 0 || b != 0` 排除且只排除終止行。每組的 carry 與 operations 都重新設為零，內層先算當前欄總和，再更新 carry，避免覆蓋上一欄帶來的值。

布林比較 `sum >= 10` 會轉成零或一，既可作下一欄進位，也可直接加到 operations。a、b 除以十後進入下一欄，即使其中一個已為零仍會繼續。最後用三種格式輸出：零次是固定句子，一次不加 s，多次才加 s，所有句子都保留句末句點。
