`fingers` 的字串列出各音符使用的手指，其中字元 `'0'` 代表第 10 根，轉成陣列索引時特別映射到 9；其他字元以 `key-'1'` 轉成從 0 開始的索引。

讀完測試筆數後，先用一次 `getline` 消耗該行剩下的換行，之後每次 `getline` 都對應完整的一首歌，包括空行。若輸入使用 CRLF，會移除行尾的 `\r`。

每首歌內建立新的計數與上一個指法陣列；每個音符內又建立新的目前指法陣列。比對完十根手指後直接指定 `previous=current`，同時處理按下與放開。輸出以索引順序列出十個數字，只有後九個數字前需要空格。
