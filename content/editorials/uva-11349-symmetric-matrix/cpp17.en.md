The header extraction stores `N` and `=` in character variables before reading `n`; formatted character extraction tolerates the surrounding spaces. A `vector<long long>` safely stores every allowed endpoint value.

The reading loop only changes `symmetric` to false and never exits early. The second loop uses `values.size()-1-i` as the 180-degree partner; the center of an odd matrix simply compares with itself. The final branch emits the complete required message, including case number and period.
