總磚塊數固定，最後每一堆只能等於算術平均 `target=total/count`。每個高於平均的堆都必須送出 `height−target` 塊；把這些多餘量相加，就是搬運次數的下界，而且能直接把每塊搬到不足的堆達成，不必模擬配對。只數過高部分，避免把來源與目的地重複計算。若所有堆已一樣高，答案自然是零。輸出要保留 `Set #` 編號、完整英文句子，以及每組後面的空白行；`count=0` 才停止。

python3 程式依上述不變量更新資料；每個分支都對應一種明確的輸入或狀態。
