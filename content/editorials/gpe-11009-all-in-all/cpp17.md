matched 是下一個需要匹配的索引，也等於已完成前綴長度；每看到 t 的一個字元最多增加一次，因此同個位置不能重用。先檢查長度再比較字元，短路求值保護字串索引。

外層 while 每次恰讀兩個以空白分隔的字串，不依賴換行位置。最後以 matched==s.size() 判斷完整匹配，不能改成 matched>0。size_t 與 string::size 的型別一致，長字串也不會因有號比較產生警告或截斷。
