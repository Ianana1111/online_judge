power3 預先保存每個完整圖的紅球數，half 用 1LL 左移計算半高。prefix 優先檢查 rows==0，確保空前綴不會落入 k==0 回傳一的分支。

rows 在上半時只乘二，不改相對列數；跨到下半時，先加完整上半的兩份圖，再把列數減掉 half，繼續處理左下圖。result 以 long long 保存兩個前綴差，不做取模，因為題目要求完整整數。Case 編號從一開始，每組各一行。
