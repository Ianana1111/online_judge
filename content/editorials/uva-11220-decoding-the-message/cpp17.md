getline 保留訊息的行邊界，istringstream 再處理行內一個或多個空白。decoded 是該行的局部字串，下一行會自動清空。判斷 word.size()>decoded.size() 後才索引，因此不會讀到字串尾端之外。

合法的非空密文行至少有一個非空單字，第一個字母一定可取，所以 decoded 為空就代表分隔空行。started 讓組首空白被跳過，而已開始讀訊息後的空行結束這組。每組先印 Case #標頭，第二組以後再於標頭之前加入空行，保留要求的區隔。
