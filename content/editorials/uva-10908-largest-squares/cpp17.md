`limit` 是中心到上、左、下、右邊界距離的最小值。外層擴張迴圈從 `next=1` 開始，因為半徑 0 本來就一定有效。

`offset` 從 `-next` 掃到 `next`，四個比較式分別涵蓋上邊、下邊、左邊與右邊。任一字元不等於中心，就把 `good` 設為 false。只有整圈檢查完仍合法，才會把 `radius` 更新成 `next`，因此失敗那一圈不會被算進答案。

讀入查詢數後，先輸出 M、N、Q，再以 `while (queries--)` 消耗查詢。每筆的半徑重新歸零，不會受到前一個中心的答案影響；最終輸出奇數邊長，連 1×1 網格也能使用同一套流程。
