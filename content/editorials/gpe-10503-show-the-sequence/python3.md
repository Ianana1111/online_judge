evaluate 用 at 移動到每層的整數，先跳過可能的負號，再讀完所有數字。遇到右括號就是最內層常數；遇到加號或乘號則保存外層操作，下一輪進入其左括號。輸入由題面保證是合法文法，所以計算階段不需要重新建立一般運算式語法樹。

加法分支用 result[:-1]，正好對應下一個外層值所需的前一個子項；乘法分支使用全部 result，並在乘完後 append。兩者都輸出剛好 count 項。程式逐行處理不同編碼，不沿用前一組的 frames 或 result，以空格連接精確整數後換行。
